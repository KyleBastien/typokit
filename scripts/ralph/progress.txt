# Ralph Progress Log
Started: 03/01/2026 21:04:22
---

## Codebase Patterns
- Use globalThis cast for crypto access (same pattern as process/fetch) — no @types/node means no global type declarations
- rstest 0.0.1 does NOT support `vi` (vitest mocks) — use manual spy patterns (e.g., `fetchCalls` array) for mocking
- ValidatorMap maps string refs to ValidatorFn — mirrors HandlerMap pattern; RouteHandler.validators is optional
- SerializerMap follows same pattern as ValidatorMap — string ref in RouteHandler.serializer pointing to SerializerFn in map
- Validation runs after route matching, before handler — prefix field errors with params./query./body. for attribution
- Use `rstest run --passWithNoTests` for test commands (rstest 0.0.1 hangs without `run` subcommand)
- Import test functions from `@rstest/core` (not `rstest`): `import { describe, it, expect } from "@rstest/core"`
- Package tsconfigs extend `../../tsconfig.base.json` — don't put `paths` in base (causes TS5090). Paths live in root `tsconfig.json` with `baseUrl`.
- All packages follow same structure: `packages/<name>/package.json`, `tsconfig.json`, `src/index.ts`
- Nx targets defined in `nx.json` targetDefaults — individual package.json scripts match
- ESLint 9 flat config at root (`eslint.config.mjs`), packages use `eslint src/`
- PowerShell: use `ConvertTo-Json`/`ConvertFrom-Json` for JSON manipulation, not `python -c` with f-strings (escaping issues)
- dist/ and *.tsbuildinfo are gitignored — only commit src/ files
- Cross-package deps: add `"workspace:*"` to package.json dependencies + `"references": [{ "path": "../<dep>" }]` to tsconfig.json
- Error subclasses need `Object.setPrototypeOf(this, new.target.prototype)` for correct instanceof checks
- ESLint enforces `import type` for type-only imports — separate value imports from type imports
- No `@types/node` in core — use `globalThis` cast for `process.env` access, not bare `process`
- Platform packages (platform-node/bun/deno) use `@types/node` in devDeps and node: imports — set all headers BEFORE res.writeHead()
- Duck-type check for third-party errors (e.g. Typia TypeGuardError) by `error.name` + property shape, not imports
- ESLint forbids inline `import("module").Type` casts — import types at top level instead
- Use port 0 in integration tests for auto-assigned ports, read via `server.address().port`
- Rust crate in transform-native: `Cargo.toml` + `build.rs` at package root, `.rs` files in `src/` alongside `.ts` files
- napi-rs v3 + swc_ecma_ast v21 uses `Wtf8Atom` for string literals — use `.to_string_lossy()` not `Display`/`format!`
- For native addon in ESM: use `createRequire` from `"module"` with an `env.d.ts` shim declaring `ImportMeta.url`
- `target/` and `*.node` are gitignored — Rust build artifacts and native addons not committed
- `napi build --platform --dts src/index.d.ts` auto-regenerates d.ts — keep NativeBindings interface in index.ts in sync with lib.rs napi exports
- BTreeMap for deterministic JSON serialization in Rust; serde `skip_serializing_if` for clean optional fields
- When adding napi exports for complex types, define separate `#[napi(object)]` structs prefixed with `Js` — convert to/from internal types in lib.rs
- `napi build --platform --dts src/index.d.ts` regenerates both `index.d.ts` and `index.js` — don't commit `index.js`
- `--js false` napi flag generates a file named `false` — add both `index.js` and `false` to gitignore for transform-native
- `buildPipeline()` writes to `.typokit/` directory — use `regenerated` flag to check if cache was hit
- `GeneratedOutput` is `{ filePath, content, overwrite }` — BuildResult.outputs expects GeneratedOutput[], not string[]
- No `@types/node` pattern: create `env.d.ts` with module declarations, use dynamic imports with inline type casts
- TypeMetadata has optional `jsdoc?: Record<string, string>` at both type level and property level — DB adapters use this for @table, @id, @generated, @unique, @default
- DB adapters GENERATE code (string output), don't import ORM libs at runtime — output is GeneratedOutput[]
- DatabaseAdapter interface is in @typokit/core (adapters/database.ts), not @typokit/types
- Plugin sidecar servers use @typokit/platform-node createServer directly — not server-native adapter

## 2026-03-02T05:04:30Z - US-001
- Initialized Nx monorepo with pnpm workspaces
- Created 26 packages per typokit-arch.md Section 12.11
- TypeScript project references with strict mode, path aliases for @typokit/*
- ESLint 9 flat config with typescript-eslint
- Rstest configured as test runner
- GitHub Actions CI workflow
- Files changed: package.json, pnpm-workspace.yaml, nx.json, tsconfig.base.json, tsconfig.json, eslint.config.mjs, .gitignore, .github/workflows/ci.yml, all 26 packages/* dirs
- **Learnings for future iterations:**
  - rstest 0.0.1 hangs in default mode — must use `rstest run --passWithNoTests`
  - Don't put `paths` in tsconfig.base.json without `baseUrl` — packages inherit it and get TS5090
  - Root tsconfig.json holds paths + project references for IDE; tsconfig.base.json is shared compiler options only
  - `@nx/js` plugin is not needed if using `nx:run-commands` executor with explicit commands
  - pnpm 10.30.3 requires corepack approval on first use
  - `*.tsbuildinfo` and `dist/` should be gitignored
---

## 2026-03-02T05:19:00Z - US-002
- Implemented all shared type interfaces in `packages/types/src/index.ts`
- Types defined: HttpMethod, TypoKitRequest, TypoKitResponse, RouteContract, PaginatedResponse, ErrorResponse, RouteHandler, CompiledRoute, CompiledRouteTable, HandlerMap, MiddlewareFn, MiddlewareChain, Logger, RequestContext, TypeMetadata, SchemaTypeMap, GeneratedOutput, SchemaChange, MigrationDraft, ServerHandle, BuildContext, BuildResult
- Files changed: packages/types/src/index.ts
- **Learnings for future iterations:**
  - The arch doc (typokit-arch.md) is the authoritative source for type shapes — match its interfaces exactly
  - CompiledRoute uses a radix tree structure: segment, children (Record), paramChild, wildcardChild, handlers
  - SchemaChange is an interface (not a union type) — used for add/remove/modify with entity + optional field
  - dist/ and *.tsbuildinfo are gitignored — only stage src/ files for commits
  - Python heredocs via `@" ... "@` work well in PowerShell for multi-line scripts
---

## 2026-03-02T05:22:00Z - US-003
- Implemented structured error class hierarchy in `packages/errors/src/index.ts`
- AppError base class with code, status, message, details + toJSON() → ErrorResponse
- Subclasses: NotFoundError (404), ValidationError (400), UnauthorizedError (401), ForbiddenError (403), ConflictError (409)
- Factory function createAppError() that returns correct subclass based on status code
- 17 unit tests covering all error classes and factory function
- Files changed: packages/errors/src/index.ts, packages/errors/src/index.test.ts, packages/errors/package.json, packages/errors/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Import test functions from `@rstest/core` (e.g. `import { describe, it, expect } from "@rstest/core"`), NOT from `rstest` — rstest injects globals but the module name for explicit import is `@rstest/core`
  - When a package depends on another workspace package (e.g. errors → types), add `"@typokit/types": "workspace:*"` to dependencies and `"references": [{ "path": "../types" }]` to tsconfig.json
  - Build dependency packages first (e.g. `pnpm run build` in types) before typechecking dependent packages
  - `Object.setPrototypeOf(this, new.target.prototype)` is needed in error subclasses for `instanceof` to work correctly with ES2022 target
---

## 2026-03-02T05:27:00Z - US-004
- Implemented ServerAdapter interface in `packages/core/src/adapters/server.ts`
- Interface methods: name, registerRoutes(), listen(), normalizeRequest(), writeResponse(), getNativeServer?()
- Method signatures match Section 6.4 of typokit-arch.md exactly
- Exported from `packages/core/src/index.ts`
- Added `@typokit/types` as workspace dependency + tsconfig reference
- Files changed: packages/core/src/adapters/server.ts (new), packages/core/src/index.ts, packages/core/package.json, packages/core/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Core interfaces go in `packages/core/src/adapters/` directory per arch doc convention
  - Use `export { type X }` syntax for re-exporting interfaces to keep clean type-only exports
  - Build dependency packages (e.g. `@typokit/types`) before typechecking dependent packages
---

## 2026-03-02T05:29:00Z - US-005
- Implemented DatabaseAdapter interface in `packages/core/src/adapters/database.ts`
- Interface methods: generate(), diff(), generateRepositories?() matching Section 7.3 of typokit-arch.md
- Added DatabaseState, TableState, ColumnState supporting interfaces (DatabaseState not in shared types, defined locally)
- Exported all types from `packages/core/src/index.ts`
- Files changed: packages/core/src/adapters/database.ts (new), packages/core/src/index.ts
- **Learnings for future iterations:**
  - DatabaseState is referenced in arch doc Section 7.3 but not defined in @typokit/types — defined locally in the adapter file
  - Database adapter interfaces follow the same pattern as ServerAdapter: import types from @typokit/types, define interface in adapters/ dir, re-export from index.ts
  - Adapter interfaces are type-only files — no runtime code, no tests needed
---

## 2026-03-02T05:31:00Z - US-006
- Implemented TypoKitPlugin interface and supporting types in `packages/core/src/plugin.ts`
- Types defined: AsyncSeriesHook<T>, BuildPipeline (with tapable hooks: beforeTransform, afterTypeParse, afterValidators, afterRouteTable, emit, done), CliCommand, InspectEndpoint, AppInstance, TypoKitPlugin
- TypoKitPlugin has lifecycle hooks: onBuild, onStart, onReady, onError, onStop, onSchemaChange, commands, inspect
- Added `@typokit/errors` as workspace dependency to core (for AppError type in onError hook)
- All types exported from `packages/core/src/index.ts`
- Files changed: packages/core/src/plugin.ts (new), packages/core/src/index.ts, packages/core/package.json, packages/core/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - AppInstance is not defined in the arch doc — created minimal interface (name, plugins, services) to be expanded by US-009 createApp
  - Plugin file imports from both @typokit/types and @typokit/errors — core now depends on both packages
  - AsyncSeriesHook follows tapable pattern: tap(name, fn) to register, call(...args) to invoke
  - CliCommand and InspectEndpoint are not explicitly defined in arch doc — inferred from plugin usage context
---

## 2026-03-02T05:35:00Z - US-007
- Implemented middleware system in `packages/core/src/middleware.ts`
- `defineMiddleware<TContext>()` function for typed middleware with context narrowing
- `executeMiddlewareChain()` runs middleware in priority order (lower first), accumulates context
- `createRequestContext()` creates context with `ctx.fail()` and placeholder `ctx.log`
- `ctx.fail(status, code, message, details?)` throws correct AppError subclass via `createAppError`
- 14 unit tests covering all acceptance criteria
- Files changed: packages/core/src/middleware.ts (new), packages/core/src/middleware.test.ts (new), packages/core/src/index.ts
- **Learnings for future iterations:**
  - ESLint requires `import type` for symbols used only as types — use separate import lines for type-only vs value imports
  - `createRequestContext()` is the factory for RequestContext — use it in tests and eventually in the server pipeline
  - `ctx.fail()` returns `never` (always throws) — use `createAppError` from @typokit/errors internally
  - Middleware chain executor spreads returned properties onto context — runtime type narrowing is just object spread
  - `globalThis.crypto.randomUUID()` is not available with ES2022 lib — use `Math.random()` fallback for request IDs
---

## 2026-03-02T05:46:00Z - US-008
- Implemented `defineHandlers<TRoutes>()` function in `packages/core/src/handler.ts`
- Types: `HandlerInput<TContract>`, `HandlerFn<TContract>`, `HandlerDefs<TRoutes>` — all infer from RouteContract generics
- Each handler receives `{ params, query, body, ctx }` with types from the corresponding RouteContract
- Handler return type enforced to match `TResponse` from contract
- 9 unit tests covering: handler definitions, typed params/query/body, ctx access, return type matching, sync handlers, single-route contracts, complex types
- Files changed: packages/core/src/handler.ts (new), packages/core/src/handler.test.ts (new), packages/core/src/index.ts
- **Learnings for future iterations:**
  - `defineHandlers` is a simple pass-through function — the real value is in the type system enforcement
  - Use `RouteContract<unknown, unknown, unknown, unknown>` as the constraint (not `any`) for the generic bound
  - ESLint enforces no unused destructured params — prefix with `_` (e.g., `_query`) for intentionally unused params in tests
  - Void contract fields use `undefined as void` when calling handlers in tests
---

## 2026-03-02T05:50:00Z - US-009
- Implemented `createApp()` factory function in `packages/core/src/app.ts`
- `createApp({ server, middleware?, routes, plugins?, logging?, telemetry? })` returns `TypoKitApp`
- `RouteGroup` interface: `{ prefix, handlers, middleware? }`
- Plugin lifecycle: `onStart` -> `server.listen()` -> `onReady`; `onStop` -> `server.close()` on `app.close()`
- `app.listen(port)` delegates to server adapter
- `app.getNativeServer()` delegates to server adapter
- `createErrorMiddleware()` auto-registered - catches `AppError` -> `ErrorResponse`, unknown -> 500
- 20 unit tests covering app creation, route registration, server delegation, plugin lifecycle order, error middleware
- Files changed: packages/core/src/app.ts (new), packages/core/src/app.test.ts (new), packages/core/src/index.ts
- **Learnings for future iterations:**
  - `AppInstance` is constructed from the server adapter name and registered plugins - keep it minimal
  - Error middleware follows the `(req, ctx, next) -> Response` pattern from `MiddlewareFn` in types, not the `Middleware` interface from middleware.ts
  - ESLint will flag unused imports including type imports - only import what's directly used in the file
  - Plugin hooks run in registration order - plugins array order matters
  - `app.close()` is safe to call without `app.listen()` - handle null serverHandle gracefully
---

## 2026-03-02T05:54:00Z - US-010
- Implemented enhanced error middleware in `packages/core/src/error-middleware.ts`
- `createErrorMiddleware(options?)` with `isDev` flag for dev/prod behavior
- AppError serialization with traceId from ctx.requestId
- Typia TypeGuardError detection via duck-typing (name + path/expected/value shape)
- Production: unknown errors redacted (500 generic), full details logged via ctx.log
- Development: unknown errors include stack trace, error name, and message
- 14 unit tests covering AppError, unknown error redaction, dev mode, and Typia validation errors
- Refactored: moved createErrorMiddleware from app.ts to error-middleware.ts; updated app.ts, app.test.ts, index.ts imports
- Files changed: packages/core/src/error-middleware.ts (new), packages/core/src/error-middleware.test.ts (new), packages/core/src/app.ts, packages/core/src/app.test.ts, packages/core/src/index.ts
- **Learnings for future iterations:**
  - Core package does not have `@types/node` — cannot use bare `process.env`, must cast through `globalThis`
  - Duck-typing third-party errors (e.g., `error.name === "TypeGuardError"`) avoids adding dependencies on external packages
  - When casting `Error` to `Record<string, unknown>`, TypeScript requires double-cast via `unknown` first
  - Moving exports between files in a package requires updating both index.ts re-exports and test file imports
---

## 2026-03-02T05:58:00Z - US-011
- Implemented @typokit/transform-typia Typia Validation Bridge
- generateValidator(typeMetadata): generates runtime validator function code from TypeMetadata
- generateValidatorBatch(types): batch generation with cross-type reference registry
- ValidatorGenerationError class with typeName and reason for descriptive failures
- Handles: primitives, unions, arrays, optional fields, nullable unions, template literal types, nested/reference types, recursive types, Record<K,V>
- splitUnionType respects generics/parens/braces/backtick nesting
- buildTemplateLiteralRegex converts template literal type strings to regex patterns
- 15 unit tests covering all acceptance criteria
- Files changed: packages/transform-typia/src/index.ts, packages/transform-typia/src/index.test.ts, packages/transform-typia/package.json, packages/transform-typia/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Typia v11 is a compile-time-only transformer — no runtime API for generating validators from schema objects. The bridge generates equivalent validation code from TypeMetadata directly.
  - Use `new Function("input", code + "\nreturn fnName(input);")` to evaluate generated validator code in tests
  - When adding workspace deps, update both package.json (`"workspace:*"`) and tsconfig.json (`references`)
  - Template literal type strings arrive with actual backticks (e.g. ` on_ `) — detect with startsWith/endsWith backtick
  - ESLint doesn't flag `new Function()` in this config — no eslint-disable comments needed
---

## 2026-03-02T06:20:00Z - US-012
- Implemented @typokit/transform-native Rust crate with napi-rs for Node.js interop
- SWC's swc_ecma_parser (v35) for TypeScript AST parsing
- `parser.rs`: parses TypeScript source files into ASTs with comments for JSDoc extraction
- `type_extractor.rs`: extracts TypeMetadata from interfaces including all JSDoc tags (@table, @id, @generated, @format, @unique, @minLength, @maxLength, @default, @onUpdate)
- `lib.rs`: napi-rs entry point exporting `parseAndExtractTypes(filePaths: string[]) → SchemaTypeMap`
- TypeScript wrapper in `src/index.ts` loads platform-specific native addon via `createRequire`
- `src/env.d.ts` provides minimal Node.js type declarations (avoids `@types/node` dependency)
- 11 Rust unit tests + 7 TypeScript integration tests
- Files changed: .gitignore, package.json, pnpm-lock.yaml, packages/transform-native/Cargo.toml, Cargo.lock, build.rs, package.json, tsconfig.json, src/lib.rs, src/parser.rs, src/type_extractor.rs, src/index.ts, src/index.d.ts, src/env.d.ts, src/index.test.ts
- **Learnings for future iterations:**
  - swc_ecma_ast v21 changed `Str.value` from `Atom` to `Wtf8Atom` (from hstr crate) — `Wtf8Atom` does NOT implement `Display`, use `.to_string_lossy()` for formatting
  - napi v3 is the current major version — features like `serde-json` and `napi4` still available
  - napi `--platform` flag generates platform-specific `.node` files named `index.<triple>.node`
  - ESM modules don't have global `require` — must use `createRequire` from Node.js `module` built-in
  - For packages without `@types/node`, create an `env.d.ts` shim declaring `module` and `ImportMeta.url`
  - JSDoc parser needs to handle multiple tags on same line (e.g., `/** @id @generated */`) — scan for all `@` positions
  - `cargo build` works alongside TypeScript build — `.rs` and `.ts` files coexist in `src/` directory
  - `target/` (Rust build) and `*.node` files should be gitignored
  - Rust edition 2024 is current — swc_ecma_parser 35 + swc_ecma_ast 21 + swc_common 19 are compatible
---

## 2026-03-02T06:23:00Z - US-013
- Implemented route_compiler.rs and openapi_generator.rs in @typokit/transform-native
- Route compiler: extracts route contracts from TS interfaces with "METHOD /path" keys, builds radix tree, serializes as TypeScript
- OpenAPI generator: converts route entries + type metadata to OpenAPI 3.1 JSON spec
- Radix tree handles static, param (:name), and wildcard (*name) segments
- Ambiguous route detection: conflicting param names at same path level produce clear errors
- Duplicate handler detection: same method at same path produces error
- Static and param children can coexist at same level (static takes priority at runtime)
- 19 new Rust unit tests + 6 new TypeScript integration tests (30 Rust total, 13 TS total)
- napi exports: compileRoutes(), generateOpenApi()
- Files changed: packages/transform-native/src/route_compiler.rs (new), packages/transform-native/src/openapi_generator.rs (new), packages/transform-native/src/lib.rs, packages/transform-native/src/index.ts, packages/transform-native/src/index.d.ts, packages/transform-native/src/index.test.ts
- **Learnings for future iterations:**
  - Route contract interfaces use string literal keys like `"GET /users/:id"` with `RouteContract<TParams, TQuery, TBody, TResponse>` type annotation
  - SWC parses string property keys as `Expr::Lit(Lit::Str(s))` — use `s.value.to_string_lossy().to_string()` to extract
  - `napi build --platform --dts src/index.d.ts` auto-regenerates the .d.ts file — manual edits are overwritten
  - The `NativeBindings` interface in index.ts must be kept in sync with the napi exports in lib.rs
  - serde `skip_serializing_if = "Option::is_none"` is essential for clean JSON output from the radix tree
  - BTreeMap gives deterministic ordering for serialized output (vs HashMap)
  - RouteTypeInfo enum provides rich type info for OpenAPI generation — much better than stringified types for inline object types
---

## 2026-03-02T06:38:00Z - US-014
- Implemented schema_differ.rs, test_stub_generator.rs, and typia_bridge.rs in @typokit/transform-native
- Schema differ: diffs two SchemaTypeMap versions (old vs new), produces MigrationDraft with SQL DDL stubs
  - Detects added/removed entities, added/removed/modified fields
  - Marks destructive changes (DROP TABLE, DROP COLUMN, ALTER TYPE) with `-- DESTRUCTIVE: requires review`
  - Generates CREATE TABLE, ALTER TABLE ADD/DROP/ALTER COLUMN SQL
- Test stub generator: generates TypeScript contract test scaffolding from route entries
  - Produces describe/it blocks per route with valid request, missing fields, and path parameter tests
  - Generates sample payloads from RouteTypeInfo (object literals, primitives, etc.)
- Typia bridge: prepares type metadata for JS callback, collects validator output into file path map
  - `prepare_validator_inputs()` converts TypeMetadata to serializable format sorted alphabetically
  - `collect_validator_outputs()` maps type names to `.typokit/validators/<kebab-name>.ts` paths
- napi exports: diffSchemas(), generateTestStubs(), prepareValidatorInputs(), collectValidatorOutputs()
- 20 new Rust unit tests (52 total), 8 new TypeScript integration tests (21 total)
- Files changed: packages/transform-native/src/schema_differ.rs (new), packages/transform-native/src/test_stub_generator.rs (new), packages/transform-native/src/typia_bridge.rs (new), packages/transform-native/src/lib.rs, packages/transform-native/src/index.ts, packages/transform-native/src/index.d.ts, packages/transform-native/src/index.test.ts
- **Learnings for future iterations:**
  - Schema differ takes internal TypeMetadata (from type_extractor.rs) not the napi JsTypeMetadata — lib.rs has a `js_types_to_internal()` helper to convert
  - `napi build --platform --dts src/index.d.ts` regenerates index.d.ts and index.js — the index.js loader file appears in root, not committed
  - When adding napi exports for complex types (MigrationDraft, SchemaChange), define separate `#[napi(object)]` structs prefixed with `Js` to avoid naming conflicts with internal Rust types
  - Test stub generator reuses RouteEntry and RouteTypeInfo from route_compiler.rs — no need to re-parse
  - HashMap properties in Rust produce non-deterministic JSON — use sorted Vec keys for deterministic output when order matters
  - `collect_validator_outputs` uses `Vec<Vec<String>>` for napi (not tuples) since napi doesn't support tuple types
---

## 2026-03-02T06:47:00Z - US-015
- Implemented full output pipeline in `output_pipeline.rs` with `run_pipeline()` and `compute_content_hash()`
- Content-hash caching: SHA-256 over sorted file paths + contents, stored in `.typokit/.cache-hash`
- `buildPipeline()` TypeScript function orchestrates native pipeline + writes to `.typokit/` directory:
  - `.typokit/routes/compiled-router.ts` — Compiled radix tree
  - `.typokit/schemas/openapi.json` — OpenAPI 3.1.0 spec
  - `.typokit/tests/contract.test.ts` — Contract test stubs
  - `.typokit/validators/*.ts` — Typia validators (via callback)
  - `.typokit/client/` — Reserved for client generation
- Platform-specific binaries: added `optionalDependencies` for 6 targets (win32-x64, darwin-x64, darwin-arm64, linux-x64-gnu, linux-arm64-gnu, linux-x64-musl)
- Extended `env.d.ts` with `path`, `fs`, `os` module type declarations
- Performance: 50 types + 20 routes completes in ~20ms (well under 500ms threshold)
- Files changed: packages/transform-native/src/output_pipeline.rs (new), packages/transform-native/src/lib.rs, packages/transform-native/src/index.ts, packages/transform-native/src/index.d.ts, packages/transform-native/src/index.test.ts, packages/transform-native/src/env.d.ts, packages/transform-native/Cargo.toml, packages/transform-native/Cargo.lock, packages/transform-native/package.json, .gitignore
- **Learnings for future iterations:**
  - `napi build --platform --dts src/index.d.ts --js false` generates a file literally named `false` instead of suppressing JS output — add both `index.js` and `false` to gitignore
  - `sha2` + `hex` crates work well for content hashing in Rust; deterministic hash requires sorting file paths before hashing
  - Dynamic imports (`await import("path")`) work in ESM for Node.js built-ins without @types/node — just need module declarations in env.d.ts
  - Pipeline returns structured result from Rust; TS layer handles file I/O and cache management for better testability
  - Cache hit returns empty results immediately — callers should check `regenerated` flag
  - `optionalDependencies` in package.json is the standard napi-rs pattern for distributing platform binaries
---

## 2026-03-02T06:55:00Z - US-016
- Implemented @typokit/cli Build Command
- `src/index.ts`: Main CLI entry point with arg parsing (parseArgs, run)
- `src/bin.ts`: Binary entry point for `typokit` command
- `src/logger.ts`: Structured CLI logger with verbose mode support
- `src/config.ts`: Config loading from typokit.config.ts or package.json `typokit` field
- `src/commands/build.ts`: Build command orchestrator — resolves file patterns, runs transform-native pipeline, invokes TypeScript compiler
- `src/env.d.ts`: Node.js type declarations (path, fs, child_process, url, module)
- `src/index.test.ts`: 18 unit tests covering arg parsing, config, logger, build errors, integration
- Files changed: packages/cli/src/ (7 new files), packages/cli/package.json, packages/cli/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - `GeneratedOutput` is `{ filePath, content, overwrite }` not `string` — BuildResult.outputs uses GeneratedOutput[]
  - No `@types/node` in CLI package either — use env.d.ts shims and dynamic imports with type casts
  - `spawnSync` with `shell: true` triggers DEP0190 deprecation — use without shell option
  - `URL` type is not available without `@types/node` — cast `pathToFileURL` return to `{ href: string }` instead
  - ESLint `consistent-type-imports` rule requires `import type` for type-only imports — keep type imports separate
  - Glob pattern matching uses simple regex conversion — avoid complex patterns that need a full glob library
- No `@types/node` timer APIs: cast `globalThis as unknown as { setTimeout: ..., clearTimeout: ... }` to access setTimeout/setInterval
---

## 2026-03-02T07:04:00Z - US-017
- Implemented `typokit dev` command with file watching and incremental rebuilds
- `src/commands/dev.ts`: Dev command with DevServerState, file watcher (fs.watch + polling fallback), dependency graph, AST cache, incremental rebuild, graceful shutdown (SIGINT/SIGTERM)
- `src/dev.test.ts`: 25 unit tests covering all dev command utilities
- Updated `src/index.ts`: Added dev command registration, help text, --debug-port flag parsing
- Updated `src/env.d.ts`: Added `fs.watch` type declaration and `mtimeMs` to `statSync` return type
- Files changed: packages/cli/src/commands/dev.ts (new), packages/cli/src/dev.test.ts (new), packages/cli/src/index.ts, packages/cli/src/env.d.ts
- **Learnings for future iterations:**
  - No `@types/node` means `setTimeout`/`setInterval`/`clearTimeout`/`clearInterval` are not available -- cast through `globalThis as unknown as { setTimeout: ... }` to access timer APIs
  - `fs.watch` with `{ recursive: true }` works on Windows/macOS but may not on Linux -- always provide polling fallback
  - `statSync` returns `mtimeMs` (milliseconds) for efficient change detection -- add to env.d.ts shim
  - ESLint enforces no unused destructured vars -- avoid destructuring variables you don't use
  - Dev command state machine: createDevState -> initial build -> start watcher -> incremental rebuilds -> graceful shutdown
  - File watcher debounce (50ms) batches rapid saves into single rebuilds
---

## 2026-03-02T07:10:00Z - US-018
- Implemented 9 inspect subcommands in `packages/cli/src/commands/inspect.ts`
- Subcommands: routes, route <key>, middleware, dependencies/deps, schema <name>, errors --last N, performance --route <path>, server, build-pipeline
- All support `--json` and `--format json` flags for structured JSON output (default: human-readable)
- Build-time commands read from `.typokit/` directory (routes from compiled-router.ts, schema from openapi.json)
- Runtime commands (errors, performance, server) connect to debug sidecar HTTP endpoint
- Added `http` module declaration to `env.d.ts`
- Updated `index.ts` with inspect command routing, help text, and exports
- 28 unit tests covering all subcommands, error cases, and JSON output validation
- Files changed: packages/cli/src/commands/inspect.ts (new), packages/cli/src/inspect.test.ts (new), packages/cli/src/index.ts, packages/cli/src/env.d.ts
- **Learnings for future iterations:**
  - `import.meta.url` doesn't resolve correctly in rstest for source-relative path resolution — use `process.cwd()` via globalThis cast instead
  - ESLint enforces `_` prefix for unused function parameters — use `_config` not `config` when param is required by interface but unused
  - `inspectDependencies` reads workspace package.json files for `@typokit/*` deps — works on any monorepo structure
  - Debug sidecar commands (errors, performance, server) gracefully fail with helpful messages when server isn't running
  - `http.get` from Node.js built-in doesn't need `@types/node` — just add module declaration to env.d.ts
---

## 2026-03-02T07:19:00Z - US-019
- Implemented 4 generate subcommands in `packages/cli/src/commands/generate.ts`
- `generate:db`: extracts types via transform-native, diffs empty schema to generate full DDL, writes schema.sql and schema-types.json
- `generate:client`: compiles routes, generates type-safe fetch client with createClient(), param substitution, query serialization
- `generate:openapi`: generates OpenAPI 3.1 spec with --output <path> flag support
- `generate:tests`: regenerates contract test stubs from route schemas
- `executeGenerate()` dispatcher routes to correct generator
- Updated `index.ts` with generate command routing, help text, and exports
- 20 unit tests covering all subcommands, client code generation, CLI parsing, result shapes
- Files changed: packages/cli/src/commands/generate.ts (new), packages/cli/src/generate.test.ts (new), packages/cli/src/index.ts
- **Learnings for future iterations:**
  - Generate commands reuse the same `resolveFilePatterns` helper as build.ts — consider extracting to shared utility
  - `diffSchemas({}, types, name)` with empty old schema generates full CREATE TABLE DDL for all types
  - ESLint catches unused imports even for type-only imports — don't import types unless directly used
  - `generateClientCode()` is a pure function that takes compiled route string and returns client module code — easy to test
  - `command.startsWith("generate:")` pattern in run() cleanly handles colon-separated subcommands
---

## 2026-03-02T07:24:00Z - US-020
- Implemented migrate:generate, migrate:diff, and migrate:apply subcommands
- migrate:generate: detects type changes via diffSchemas, generates timestamped SQL migration files with metadata JSON
- migrate:diff: shows pending schema changes as human-readable output or JSON (--json flag)
- migrate:apply: applies pending migrations with destructive change blocking (--force to override)
- Destructive changes (DROP, type changes) annotated with '-- DESTRUCTIVE: requires review' comments
- 17 unit tests covering dispatcher, utility functions, CLI arg parsing
- Files changed: packages/cli/src/commands/migrate.ts (new), packages/cli/src/migrate.test.ts (new), packages/cli/src/index.ts (updated exports + routing)
- **Learnings for future iterations:**
  - CLI commands follow colon-separated pattern (migrate:generate) not subcommand pattern (migrate generate) - match generate:* style
  - MigrateResult extends GenerateResult pattern with additional destructive + changes fields
  - SchemaChange and MigrationDraft types from @typokit/types are the canonical interfaces for migration data
  - Migration files use timestamp prefix (YYYYMMDDHHMMSS) for ordering - .applied file tracks which have been applied
  - ESLint catches unused type imports - use import type only for types actually referenced
---

## 2026-03-02T07:29:00Z - US-021
- Implemented scaffold commands: typokit init, typokit add route, typokit add service
- `typokit init [name]`: creates new project from template with package.json, tsconfig.json, src/app.ts, src/types.ts, directory structure matching Section 4.4 (routes/, middleware/, services/)
- `typokit add route <name>`: scaffolds contracts.ts + handlers.ts + middleware.ts in src/routes/<name>/
- `typokit add service <name>`: scaffolds <name>.service.ts in src/services/
- Templates include CRUD route contracts, handler stubs with default export for app.ts registration, middleware skeleton
- Server/DB adapter selection via --server and --db flags on init
- 19 unit tests covering utility functions, template generation, dispatcher, and arch doc compliance
- Files changed: packages/cli/src/commands/scaffold.ts (new), packages/cli/src/scaffold.test.ts (new), packages/cli/src/index.ts (updated exports + routing + help text)
- **Learnings for future iterations:**
  - Scaffold commands use `init` and `add` as top-level commands, not colon-separated (unlike generate:* and migrate:*)
  - `add` uses positional[0] as subcommand type (route/service) and positional[1] as the name
  - Templates reference @typokit/types for RouteContract, RouteHandler, RequestContext, MiddlewareFn
  - Section 4.4 convention: routes/<name>/contracts.ts + handlers.ts + middleware.ts; services/<name>.service.ts
  - All 127 tests pass (6 test files), typecheck and lint clean
---

## 2026-03-02T07:33:25Z - US-022
- Implemented test commands in `packages/cli/src/commands/test.ts`
- Three subcommands: `test` (all), `test:contracts`, `test:integration`
- Auto-detection of test runner by checking for jest.config.*, vitest.config.*, rstest.config.* files
- `--runner <jest|vitest|rstest>` flag overrides auto-detection
- Schema change detection triggers contract test regeneration before running
- Exit code reflects test runner exit code
- Wired into `index.ts` run() function with help text
- 21 unit tests in `test.test.ts`
- Files changed: packages/cli/src/commands/test.ts, packages/cli/src/test.test.ts, packages/cli/src/index.ts
- **Learnings for future iterations:**
  - Test command follows same pattern as other CLI commands: dynamic imports for fs/path/child_process, CliLogger + TypoKitConfig options
  - ESLint enforces no-unused-vars strictly — destructured but unused imports from dynamic import will fail lint
  - schemasChanged uses .typokit/build-cache.json and __generated__/ dir existence to detect staleness
  - spawnSync with ENOENT errors when test runner binary not found — handle gracefully
---

## 2026-03-02T07:37:50Z - US-023
- Implemented tapable hook system: AsyncSeriesHookImpl class + createBuildPipeline() factory
- AsyncSeriesHookImpl executes taps in registration order, supports async, exposes taps for introspection
- createBuildPipeline() creates all 6 hook phases: beforeTransform, afterTypeParse, afterValidators, afterRouteTable, emit, done
- getPipelineTaps() introspection helper returns all registered taps with hook name and order
- BUILD_HOOK_PHASES constant for iteration over phases
- Updated CLI build command to create pipeline, let plugins tap via onBuild(), fire hooks at correct phases
- Updated inspect build-pipeline to show registeredTaps array
- Added @typokit/core as CLI dependency + tsconfig reference
- 19 new unit tests (76 total in core, 148 in CLI)
- Files changed: packages/core/src/hooks.ts (new), packages/core/src/hooks.test.ts (new), packages/core/src/index.ts, packages/cli/src/commands/build.ts, packages/cli/src/commands/inspect.ts, packages/cli/package.json, packages/cli/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - CompiledRoute fields (paramChild, wildcardChild, children, handlers) are all optional — use undefined not null
  - CLI needs core rebuilt (pnpm run build in core) before typecheck works due to dist/ exports
  - BuildPipelineInstance type is the concrete implementation; BuildPipeline is the interface from plugin.ts
  - Dynamic imports with type assertions need the target package's dist/ to exist for typecheck
  - AsyncSeriesHookImpl.taps is readonly array — enables introspection without separate tracking
---

## 2026-03-02T07:44:00Z - US-024
- Implemented @typokit/platform-node — Node.js Platform Adapter
- createServer() wrapping node:http dispatches to a TypoKit request handler
- normalizeRequest() parses IncomingMessage → TypoKitRequest (method, path, headers, query, JSON body)
- writeResponse() serializes TypoKitResponse → ServerResponse (status, headers, JSON body auto-serialization)
- getPlatformInfo() returns { runtime: "node", version: process.version }
- PlatformInfo, NodeRequestHandler, NodeServerOptions, NodeServer interfaces exported
- 7 unit tests: getPlatformInfo, normalizeRequest (GET + POST/JSON), writeResponse, createServer lifecycle, error handling, graceful close
- Files changed: packages/platform-node/package.json, tsconfig.json, src/index.ts, src/index.test.ts, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Platform adapters provide runtime-specific HTTP module access — they're consumed by server adapters (e.g., server-native)
  - Must set all headers (including auto content-type for JSON) BEFORE calling res.writeHead() — Node.js throws if headers set after writeHead
  - Use port 0 and server.address() in tests for dynamic port allocation — avoids port conflicts
  - @types/node goes in devDependencies for platform-node — this is the only package that directly uses node: imports
  - Platform adapters are orthogonal to server adapters per arch doc Section 6.5
---

## 2026-03-02T07:49:00Z - US-025
- Implemented @typokit/server-native with nativeServer() factory returning ServerAdapter
- Radix tree traversal for O(k) route lookup: static children, paramChild, wildcardChild
- 404 response for unmatched routes, 405 with Allow header for wrong HTTP method
- Trailing slash normalization (/users/ matches /users)
- normalizeRequest/writeResponse delegating to @typokit/platform-node
- 14 integration tests covering routing, params, POST body, 404/405, trailing slashes
- Files changed: packages/server-native/src/index.ts, packages/server-native/src/index.test.ts, packages/server-native/package.json, packages/server-native/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - ServerAdapter.normalizeRequest is sync but platform-node's is async — handle async normalization inside the server's request handler, expose sync normalizeRequest for already-parsed objects
  - Use port 0 in tests to get auto-assigned ports, then read via server.address().port
  - ESLint forbids inline import("module").Type casts — import the type at top level instead
  - The MiddlewareChain from types has entries with { name, handler: MiddlewareFn } but core's executeMiddlewareChain expects MiddlewareEntry[] with { name, middleware: Middleware } — need to adapt between the two
---

## 2026-03-02T07:54:00Z - US-026
- Implemented request validation pipeline in @typokit/server-native
- Added validation types to @typokit/types: ValidatorFn, ValidatorMap, ValidationResult, ValidationFieldError
- Added optional alidators field to RouteHandler interface (params/query/body string refs)
- Updated ServerAdapter.registerRoutes in @typokit/core to accept optional ValidatorMap
- Implemented unValidators() function that validates params, query, body before handler execution
- Validation errors produce 400 ErrorResponse with field-level details, prefixed with params./query./body.
- Multiple validator failures are aggregated into a single response
- Backwards compatible: no validators = no validation (existing behavior preserved)
- Exported unValidators and alidationErrorResponse utilities for reuse
- Files changed: packages/types/src/index.ts, packages/core/src/adapters/server.ts, packages/server-native/src/index.ts, packages/server-native/src/index.test.ts
- **Learnings for future iterations:**
  - ValidatorMap is a Record<string, ValidatorFn> mapping validator refs to runtime functions — mirrors HandlerMap pattern
  - RouteHandler.validators is optional with optional params/query/body fields — non-breaking addition
  - Validation runs BEFORE handler execution but AFTER route matching and param extraction
  - Field errors are prefixed with their source (params./query./body.) for clear error attribution
  - ESLint enforces no unused imports — don't import types unless directly referenced in code (ValidatorFn was imported but only used in ValidatorMap type)
  - ServerAdapter.registerRoutes 4th param is optional — backwards compatible with existing adapters
---

## 2026-03-02T08:02:10Z - US-027
- Implemented response serialization pipeline for @typokit/server-native
- Added SerializerFn/SerializerMap types to @typokit/types
- Added optional `serializer` field to RouteHandler interface
- Updated ServerAdapter.registerRoutes in @typokit/core to accept serializerMap
- Implemented serializeResponse() with compiled schema lookup + JSON.stringify fallback
- Content-Type: application/json set automatically for JSON bodies
- Files changed: packages/types/src/index.ts, packages/core/src/adapters/server.ts, packages/server-native/src/index.ts, packages/server-native/src/index.test.ts
- **Learnings for future iterations:**
  - SerializerMap follows same pattern as ValidatorMap — string ref in RouteHandler pointing to a map entry
  - Response serialization happens after handler, before writeResponse — body becomes a pre-serialized string
  - writeResponse in platform-node passes string bodies through unchanged (no double-serialization)
  - Must build dependent packages (types, core) before typecheck on downstream packages
---

## 2026-03-02T08:08:00Z - US-028
- Implemented DrizzleDatabaseAdapter in packages/db-drizzle/src/index.ts
- PostgreSQL: pgTable, uuid, varchar, text, integer, bigint, boolean, timestamp, jsonb, pgEnum
- SQLite: sqliteTable, text, integer with mode mappings
- JSDoc tags: @table, @id, @generated (uuid/now), @format, @unique, @default, @onUpdate, @maxLength
- diff() compares SchemaTypeMap against DatabaseState, produces MigrationDraft with SQL
- Extended TypeMetadata in @typokit/types with optional jsdoc fields (backward compatible)
- 18 unit tests all passing
- Files changed: packages/types/src/index.ts, packages/db-drizzle/src/index.ts, packages/db-drizzle/src/index.test.ts, packages/db-drizzle/package.json, packages/db-drizzle/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - TypeMetadata now has optional jsdoc?: Record<string, string> at both type and property level — use this for DB adapter JSDoc tag support
  - String union types like "a" | "b" | "c" in TypeMetadata.properties[x].type map to pgEnum in PostgreSQL
  - DB adapters don't import drizzle-orm at runtime — they GENERATE code that uses it
  - toSnakeCase + pluralize convention for table names when no @table JSDoc tag
  - DatabaseAdapter interface is in @typokit/core (not @typokit/types) — add both as workspace deps
- Plugin sidecar servers use @typokit/platform-node createServer directly — not server-native adapter
---

## 2026-03-02T08:17:00Z - US-029
- Implemented @typokit/db-kysely Kysely type generation adapter
- KyselyDatabaseAdapter implements DatabaseAdapter interface
- generate() produces a single database.ts file with table interfaces and Database interface
- Maps TS types to Kysely column types for PostgreSQL (native types) and SQLite (number/string)
- Uses Generated<T> wrapper for auto-generated and default columns
- Handles all JSDoc metadata tags (@id, @generated, @default, @unique, @table, @onUpdate)
- diff() produces migration drafts identical to db-drizzle pattern
- 19 unit tests covering all column types, dialects, unions, diff scenarios
- Files changed: packages/db-kysely/src/index.ts, packages/db-kysely/src/index.test.ts, packages/db-kysely/package.json, packages/db-kysely/tsconfig.json
- **Learnings for future iterations:**
  - Kysely uses TypeScript interfaces (not ORM calls) for schema — Generated<T> wraps columns with defaults/auto-generation
  - db-kysely generates a single database.ts file (not per-table files like Drizzle)
  - SQLite in Kysely maps boolean→number and Date→string since SQLite lacks those types
  - The adapter pattern (DatabaseAdapter interface, helpers, diff logic) is identical across all db-* packages
---

## 2026-03-02T08:21:51Z - US-030
- Implemented PrismaDatabaseAdapter in `packages/db-prisma/src/index.ts`
- Generates schema.prisma file with datasource, generator, enums, and models
- Maps TS types to Prisma field types: String, Int, BigInt, Boolean, DateTime, Json
- Handles all JSDoc metadata: @id, @default (uuid/now/autoincrement/literal), @unique, @generated, @onUpdate
- Generates Prisma enums from string union types (e.g. `"a" | "b"` -> `enum TypeProp`)
- Uses @@map for table name mapping, @map for column name mapping
- diff() produces MigrationDraft with SQL migration statements
- 18 unit tests covering all types, enums, defaults, diff operations, and constructor options
- Files changed: packages/db-prisma/src/index.ts, packages/db-prisma/src/index.test.ts, packages/db-prisma/package.json, packages/db-prisma/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Prisma schema uses model/enum keywords (not CREATE TABLE) — it's its own DSL, not SQL
  - Prisma field annotations use @id, @default(), @unique, @updatedAt, @map() syntax
  - Use @@map("table_name") at model level for custom table names
  - PascalCase enum names (e.g. UserStatus) follow Prisma conventions vs snake_case in Drizzle
  - ESLint enforces no unused vars — use _ prefix for intentionally unused parameters
---

## 2026-03-02T08:25:00Z - US-031
- Implemented @typokit/db-raw package with RawSqlDatabaseAdapter class
- generate() produces SQL CREATE TABLE DDL files (.sql) and TypeScript interfaces (types.ts)
- PostgreSQL dialect: CREATE TYPE for enums, UUID, TIMESTAMPTZ, VARCHAR(n), BIGINT, BOOLEAN, JSONB, TEXT
- SQLite dialect: INTEGER, TEXT only (no CREATE TYPE, boolean → INTEGER)
- diff() produces ALTER TABLE migration drafts with DESTRUCTIVE comment flags
- 21 unit tests covering all column types, enums, constraints, both dialects, diff detection
- Files changed: packages/db-raw/src/index.ts, packages/db-raw/src/index.test.ts, packages/db-raw/package.json, packages/db-raw/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - DB adapter packages all follow identical structure: import types from @typokit/types and DatabaseAdapter+DatabaseState from @typokit/core
  - Raw SQL adapter generates .sql files (one per table) + a types.ts file with TS interfaces
  - ESLint catches unused variables — don't compute values you don't use in the final output
  - Pattern: helpers (toSnakeCase, pluralize, toTableName, toColumnName, parseUnionValues) are shared across all DB adapters
---

## 2026-03-02T08:31:10Z - US-032
- Implemented createTestClient(app) function in @typokit/testing
- Test client starts app on port 0 (random), returns typed HTTP client
- Client methods: get, post, put, patch, delete + contract-typed request()
- Options: body, query, headers; Response: status, body (parsed JSON), headers
- Integration test with 10 test cases against nativeServer sample app
- Files changed: packages/testing/src/index.ts, packages/testing/src/index.test.ts, packages/testing/package.json, packages/testing/tsconfig.json
- **Learnings for future iterations:**
  - getNativeServer() returns raw node:http Server — use .address().port to get the actual port
  - Testing package needs @typokit/server-native + @typokit/platform-node as devDeps for integration tests
  - Use port 0 pattern for auto-assigned ports, read via nativeServer.address().port
  - ESLint catches unused imports in test files — prefix unused params with _
---

## 2026-03-02T08:37:51Z - US-033
- Implemented `createIntegrationSuite(app, options)` function in `packages/testing/src/integration-suite.ts`
- Creates isolated server + in-memory database per suite instance
- Options: `database: boolean` to spin up in-memory DB, `seed: string` for named seed fixtures
- `suite.setup()` starts server and seeds DB, `suite.teardown()` stops server and cleans up
- `suite.client` returns typed test client, `suite.db` returns in-memory database
- Includes `registerSeed()` / `getSeed()` for named seed fixture registration
- InMemoryDatabase supports insert, findAll, findById, clearTable, clear, tables
- Returns copies from findAll/findById to prevent mutation of internal state
- Files changed: packages/testing/src/integration-suite.ts (new), packages/testing/src/integration-suite.test.ts (new), packages/testing/src/index.ts (re-exports)
- 25 tests pass (15 new integration-suite tests + 10 existing test-client tests)
- **Learnings for future iterations:**
  - Integration suites reuse `createTestClient` from existing test client module
  - Each suite creates its own app instance — do not share app objects between suites
  - In-memory database is a simple Map-based store, not tied to any ORM adapter
  - Seed registry is global (module-level Map) — register seeds before creating suites
---

## 2026-03-02T08:42:00Z - US-034
- Implemented createFactory<T>(metadata, options) in packages/testing/src/factory.ts
- Factory.build(overrides?) produces valid instances with random data from TypeMetadata
- Factory.buildMany(count, overrides?) produces multiple instances
- Factory.buildInvalid(field) produces instance with specific field invalid
- Seeded PRNG (mulberry32) for deterministic output (same seed = same data)
- JSDoc constraint support: @format email/url/uuid/date-time, @minLength, @maxLength, @minimum, @maximum
- String union type support (picks random valid value from union)
- Array type support (string[] generates array of strings)
- 17 unit tests covering: valid build, formats, constraints, overrides, buildMany, buildInvalid, determinism, arrays
- Files changed: packages/testing/src/factory.ts (new), packages/testing/src/factory.test.ts (new), packages/testing/src/index.ts
- **Learnings for future iterations:**
  - TypeMetadata.properties[x].jsdoc holds constraint tags for data generation
  - mulberry32 is a simple seedable PRNG that works well for deterministic test data
  - Factory uses a fresh PRNG per build() call from same seed for determinism; buildMany uses single PRNG for variety
  - String union types can be parsed by splitting on | and stripping quotes
  - ESLint is clean with import type for type-only imports from @typokit/types
---

## 2026-03-02T08:48:00Z - US-035
- Implemented toMatchSchema(schemaName) custom matcher in packages/testing/src/schema-matcher.ts
- registerSchemaValidators() registers compiled ValidatorFn instances by schema name
- getSchemaValidator() retrieves by name with descriptive errors listing available schemas
- matchSchema() core logic validates input and produces field-level error details
- toMatchSchema() returns { pass, message() } compatible with Jest, Vitest, and Rstest expect.extend()
- Clear error messages show path, expected type, and actual value for each failing field
- 19 unit tests covering: registration, retrieval errors, valid PublicUser pass, invalid fail, field error details, .not context, expect.extend integration
- Files changed: packages/testing/src/schema-matcher.ts (new), packages/testing/src/schema-matcher.test.ts (new), packages/testing/src/index.ts (exports)
- **Learnings for future iterations:**
  - Jest/Vitest/Rstest all share the same expect.extend({ matcherName: fn }) API — matcher returns { pass, message() }
  - The 	his context in matchers has isNot for .not assertions — but the framework handles negation automatically
  - ValidatorFn returns ValidationResult with errors?: ValidationFieldError[] — path, expected, actual fields
  - Rstest's expect.extend works identically to Jest/Vitest — no adapter differences needed
---

## 2026-03-02T08:52:00Z - US-036
- Implemented generateContractTests() in packages/testing/src/contract-generator.ts
- Routes grouped by path prefix into __generated__/*.contract.test.ts files
- Test-runner-agnostic: supports Jest, Vitest, Rstest import styles via runner option
- detectTestRunner() auto-detects runner from package.json deps/scripts
- For each route generates: valid input → expected status, missing required fields → 400, invalid field formats → 400
- Tests use createTestClient(app) and toMatchSchema() from @typokit/testing
- Idempotent: same schema always generates identical output (deterministic sorting)
- DO NOT EDIT header on all generated files
- Supports custom expectedStatus, optional responseSchema assertions
- Handles GET (no body) and POST/PUT/PATCH (with body) routes differently
- 27 unit tests covering all acceptance criteria
- Files changed: packages/testing/src/contract-generator.ts (new), packages/testing/src/contract-generator.test.ts (new), packages/testing/src/index.ts (exports)
- **Learnings for future iterations:**
  - Contract test generator works with ContractTestRoute[] + SchemaTypeMap — decoupled from compile step
  - Group routes by first path segment for file organization (e.g., /users/* → users.contract.test.ts)
  - Jest imports from @jest/globals, Vitest from vitest, Rstest from @rstest/core
  - ESLint catches unused type imports in test files — only import types actually used
  - beforeAll/afterAll pattern for test client lifecycle avoids spinning up a server per test
  - Invalid value generation: use "not-an-email" for email format, wrong-type values for number/boolean
---

## 2026-03-02T08:59:00Z - US-037
- Implemented type-safe fetch client in `packages/client/src/index.ts`
- createClient<TRoutes>(options) factory creating TypeSafeClient with typed get/post/put/patch/delete methods
- ExtractParams<T> type-level utility for inferring path params from route patterns
- RouteMap type mapping paths to per-method RouteContracts
- Request interceptors (sync and async) for auth headers etc.
- ClientError class extending AppError for non-OK responses
- Error responses with ErrorResponse body throw typed AppError subclasses via createAppError
- env.d.ts declares fetch/URL/RequestInit/Response types for platform-agnostic builds
- 13 unit tests: client construction, GET/POST/PUT/DELETE, path params, query params, JSON body, headers, error handling, interceptors
- Files changed: packages/client/package.json, packages/client/tsconfig.json, packages/client/src/index.ts, packages/client/src/index.test.ts, packages/client/src/env.d.ts
- **Learnings for future iterations:**
  - rstest 0.0.1 does NOT support `vi` (vitest mock utilities) — use manual fetch spy pattern with `fetchCalls` array instead
  - For platform-agnostic packages needing fetch/URL/Headers, create `env.d.ts` with ambient type declarations (no DOM lib, no @types/node)
  - Use `ResponseLike` interface instead of `Response` in env.d.ts to avoid conflicts with any lib that does declare Response
  - Type-level conditional types for MethodRequestOptions get complex — test with explicit type assertions in test file
---

## 2026-03-02T09:06:07Z - US-038
- Implemented StructuredLogger class in packages/otel/src/logger.ts implementing Logger interface from @typokit/types
- ctx.log API with all six levels: trace, debug, info, warn, error, fatal
- Log entries automatically include: traceId, route, phase, requestId, serverAdapter name
- Log entries are structured JSON (LogEntry type in types.ts)
- Default sink: StdoutSink writes JSON to stdout (zero config)
- Log level configurable via LoggingConfig.level (defaults: info in production, debug in development)
- Sensitive field redaction via LoggingConfig.redact (glob-style patterns like *.password)
- Child logger support via .child() for adding metadata per-phase
- Multiple sinks support (TestSink for testing, StdoutSink for production)
- 17 unit tests covering all levels, metadata enrichment, level filtering, redaction (nested + arrays), child loggers, multi-sink
- Files changed: packages/otel/package.json, packages/otel/tsconfig.json, packages/otel/src/index.ts, packages/otel/src/types.ts, packages/otel/src/logger.ts, packages/otel/src/redact.ts, packages/otel/src/index.test.ts
- **Learnings for future iterations:**
  - @typokit/otel depends on @typokit/types for the Logger interface — keep StructuredLogger in sync with Logger
  - Use globalThis cast pattern for process.env/process.stdout access (no @types/node in this package)
  - LogSink interface enables testability — inject TestSink[] to capture entries without stdout noise
  - redactFields works recursively on nested objects and arrays of objects
  - StdoutSink uses process.stdout.write (not console.log) to avoid extra formatting
---

## 2026-03-02T09:10:40Z - US-039
- Implemented OpenTelemetry tracing in packages/otel/src/tracing.ts
- Span class: mutable span with traceId, spanId, parentSpanId, name, kind, attributes, status, timing
- Tracer class: manages spans per request — startRootSpan(), startSpan(), flush(), getSpans()
- Span hierarchy: root span (server kind) → child spans (internal kind) for middleware, validation, handler, serialization
- Trace ID auto-generated (32-char hex) and propagated across all spans
- Three exporters: ConsoleSpanExporter (stdout JSON), OtlpSpanExporter (HTTP POST to OTLP endpoint), NoopSpanExporter
- resolveTracingConfig() normalizes TelemetryConfig into TracingConfig (handles boolean or object tracing option)
- createRequestTracer() factory for per-request tracer with TelemetryConfig
- TracingConfig, TelemetryConfig, SpanData, SpanStatus, SpanExporter types added to types.ts
- 36 unit tests covering: ID generation, span lifecycle, attributes, status, parent-child hierarchy, tracer flush/export, config resolution, exporter creation
- Files changed: packages/otel/src/tracing.ts (new), packages/otel/src/tracing.test.ts (new), packages/otel/src/types.ts, packages/otel/src/index.ts
- **Learnings for future iterations:**
  - Use globalThis cast pattern for crypto access (same as process): (globalThis as unknown as { crypto?: ... }).crypto
  - No @types/node means no global crypto/fetch types — always wrap with globalThis casts
  - OtlpSpanExporter uses fire-and-forget fetch with .catch(() => {}) for non-blocking export
  - SpanData is the serializable interface; Span is the mutable class that converts to SpanData via toData()
  - Tracer.flush() auto-ends any open spans before exporting
---


## 2026-03-02T09:15:07Z - US-040
- Implemented MetricsCollector class in packages/otel/src/metrics.ts
- Three metric instruments: http.server.request.duration (histogram), http.server.active_requests (gauge), http.server.error_count (counter)
- All metrics labeled with route, method, status code via MetricLabels interface
- Three metric exporters: ConsoleMetricExporter (stdout JSON), OtlpMetricExporter (HTTP POST to OTLP endpoint), NoopMetricExporter
- resolveMetricsConfig() normalizes TelemetryConfig into MetricsConfig (handles boolean or object metrics option)
- createMetricsCollector() factory for app-level metrics with TelemetryConfig
- TelemetryConfig.metrics now supports boolean | MetricsConfig (updated types.ts)
- telemetry.serviceName sets the OTel resource service name
- MetricData, MetricLabels, HistogramDataPoint, GaugeDataPoint, CounterDataPoint types added to types.ts
- 27 unit tests covering all acceptance criteria
- Files changed: packages/otel/src/metrics.ts (new), packages/otel/src/metrics.test.ts (new), packages/otel/src/types.ts, packages/otel/src/index.ts
- **Learnings for future iterations:**
  - MetricData has a 	ype field (histogram/gauge/counter) — avoid spreading MetricData into objects with a 	ype property (TS2783 duplicate property error)
  - Metric exporters follow same pattern as SpanExporters — Console/Otlp/Noop trio
  - resolveMetricsConfig mirrors resolveTracingConfig — same pattern for boolean | object normalization
  - MetricsCollector tracks active requests as a simple counter (increment on start, decrement on end, floor at 0)
---

## 2026-03-02T09:19:30Z - US-041
- Implemented OTel Log Bridge in packages/otel/src/log-bridge.ts
- OtelLogSink class implements LogSink, pushes structured log entries to OTel Collector via OTLP HTTP POST
- Log entries include traceId and spanId (from data field) for trace correlation
- Severity mapping: trace=1, debug=5, info=9, warn=13, error=17, fatal=21 per OTel spec
- Route, phase, requestId, serverAdapter sent as OTLP attributes; extra data fields prefixed with data.*
- createOtelLogSink() factory: opt-in, returns OtelLogSink only when telemetry.tracing is enabled
- Derives /v1/logs endpoint from /v1/traces endpoint automatically
- Fire-and-forget fetch with .catch(() => {}) for non-blocking export (same pattern as OtlpSpanExporter)
- Works alongside StdoutSink (both active simultaneously via multi-sink StructuredLogger)
- 17 unit tests covering: OTLP format, trace context, severity mapping, attributes, missing fetch, opt-in config, endpoint derivation, serviceName, integration with StructuredLogger
- Files changed: packages/otel/src/log-bridge.ts (new), packages/otel/src/log-bridge.test.ts (new), packages/otel/src/index.ts (exports)
- **Learnings for future iterations:**
  - OTel log severity numbers: TRACE=1, DEBUG=5, INFO=9, WARN=13, ERROR=17, FATAL=21
  - OTLP logs endpoint is /v1/logs (same base as /v1/traces and /v1/metrics)
  - spanId can be passed via LogEntry.data.spanId for correlation — the bridge extracts it as a top-level OTLP field
  - OtelLogSink follows same fire-and-forget pattern as OtlpSpanExporter and OtlpMetricExporter
  - createOtelLogSink reuses resolveTracingConfig() to determine if tracing is enabled (opt-in check)
---

## 2026-03-02T09:23:00Z - US-042
- Implemented @typokit/plugin-debug with full debug sidecar server
- debugPlugin(options) factory function implementing TypoKitPlugin interface
- Sidecar HTTP server on separate port (default 9800) using @typokit/platform-node createServer
- Dev mode: no auth, binds to 0.0.0.0; Production mode: API key via X-Debug-Key, IP/CIDR allowlist, rate limiting, binds to 127.0.0.1
- 8 debug endpoints: routes, middleware, performance (p50/p95/p99), errors, health, dependencies, traces, logs
- All read-only (405 on non-GET), with redaction support via @typokit/otel redactFields
- onSchemaChange hook clears cached route map; onError hook records errors
- Services API (_debug) for recording errors, traces, logs, performance data, route table
- 18 integration tests: lifecycle, all endpoints, production security (API key auth), redaction
- Files changed: packages/plugin-debug/src/index.ts, packages/plugin-debug/src/index.test.ts, packages/plugin-debug/package.json, packages/plugin-debug/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Plugin debug sidecar uses @typokit/platform-node createServer directly (not server-native) for a standalone HTTP server
  - otel dist may be stale — rebuild with 	sc -p tsconfig.json in packages/otel before typechecking dependents
  - Plugin exposes data collection via app.services["_debug"] — other plugins/middleware can call recordError/recordTrace/etc.
  - Use port 19800+ for test ports to avoid conflicts with other tests using default ports
  - CompiledRoute tree traversal needs to handle paramChild/wildcardChild separately from static children
---

## 2026-03-02T09:31:00Z - US-043
- Implemented @typokit/plugin-ws WebSocket support plugin
- WsChannels contract interface mapping channel names to serverToClient/clientToServer message types
- defineWsHandlers<TChannels>() function for typed handler definitions with onConnect/onMessage/onDisconnect
- wsPlugin(options) factory function implementing TypoKitPlugin interface
- Build hooks: afterTypeParse extracts WS channel contracts, emit generates validators and route table
- WsConnectionManager class for managing active connections with broadcast, channel grouping
- Message validation against clientToServer contracts with error reporting
- handleWsUpgrade() for incoming WS connection handling with validation pipeline
- extractWsChannels(), generateWsValidators(), generateWsRouteTable() build-time utilities
- requireAuth option for auth middleware integration
- 25 unit/integration tests covering all components
- Files changed: packages/plugin-ws/src/index.ts, packages/plugin-ws/src/index.test.ts, packages/plugin-ws/package.json, packages/plugin-ws/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - No @types/node in plugin packages — use globalThis cast for TextDecoder, setInterval, clearInterval
  - Plugin pattern: factory function returns TypoKitPlugin, register services in onStart via app.services
  - Build hooks use closure variables to share state between afterTypeParse and emit (BuildContext has no get/set)
  - WS channel detection uses @ws or @channel JSDoc tags on types, or direct serverToClient/clientToServer property pattern
---

## 2026-03-02T09:38:00Z - US-044
- Implemented Fastify server adapter with full ServerAdapter interface
- fastifyServer(options?) factory: options passed to Fastify constructor (logger, trustProxy, etc.)
- registerRoutes() collects routes from compiled radix tree and registers as Fastify-native routes
- normalizeRequest() converts FastifyRequest → TypoKitRequest (method, path, headers, body, query, params)
- writeResponse() converts TypoKitResponse → FastifyReply (status, headers, body)
- getNativeServer() returns raw Fastify instance for escape hatches (plugin registration etc.)
- Request validation pipeline (params/query/body validators from ValidatorMap)
- Response serialization pipeline (custom serializer from SerializerMap)
- Middleware chain executes before handler per arch Section 6.3
- 12 integration tests: routing, params, POST body, validation, serialization, middleware, 404, port 0
- Files changed: packages/server-fastify/src/index.ts, packages/server-fastify/src/index.test.ts, packages/server-fastify/package.json, packages/server-fastify/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Fastify v5 deprecates direct maxParamLength option — use outerOptions instead
  - Fastify uses pp.route({ method, url, handler }) for programmatic route registration
  - Use pp.listen({ port: 0, host: '0.0.0.0' }) for auto-port, read via 
ative.addresses()[0].port
  - Fastify automatically parses JSON bodies — no need for manual body parsing in normalizeRequest
  - Route tree traversal: collect flat route entries from radix tree (reconstructing paths) rather than doing runtime tree lookup
---

## 2026-03-02T09:45:53Z - US-045
- Implemented Hono server adapter with full ServerAdapter interface
- honoServer(options?) factory: options supports basePath
- registerRoutes() collects routes from compiled radix tree and registers as Hono-native routes via app.on(method, path)
- normalizeRequest() converts Hono Context -> TypoKitRequest (method, path, headers, body, query, params)
- writeResponse() converts TypoKitResponse -> Hono Response object
- getNativeServer() returns raw Hono instance for escape hatches
- Request validation pipeline (params/query/body validators from ValidatorMap)
- Response serialization pipeline (custom serializer from SerializerMap)
- Middleware chain executes before handler per arch Section 6.3
- Uses @hono/node-server for listen() with Node.js HTTP server
- 12 integration tests: interface check, getNativeServer, routing, params, POST body, validation, serialization, middleware, 404, port 0, options
- Files changed: packages/server-hono/src/index.ts, packages/server-hono/src/index.test.ts, packages/server-hono/package.json, packages/server-hono/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Hono uses app.on(method, path, handler) for dynamic method registration — app[method] doesn't work for 'head'
  - Hono returns Response objects from route handlers (not imperative write like Fastify/Express)
  - Hono params via c.req.param() returns all params as Record<string, string>
  - Hono body parsing: use c.req.json() (async) — must be done before normalizeRequest
  - @hono/node-server serve() returns a Node http.Server — use server.address().port for auto-port
  - Hono route patterns use :param syntax same as TypoKit compiled routes
  - For listen() with port 0, wait for 'listening' event on the Node server before returning handle
---

## 2026-03-02T09:54:00Z - US-046
- Implemented Express server adapter at packages/server-express/
- Factory function expressServer(options?) that implements ServerAdapter interface
- registerRoutes() translates compiled route table into Express-native route registrations
- normalizeRequest() converts Express Request to TypoKitRequest
- writeResponse() converts TypoKitResponse to Express Response
- getNativeServer() returns the raw Express app instance
- JSON body parsing enabled via express.json() middleware
- Uses node:http createServer for listen() with port 0 support in tests
- 8 integration tests: adapter creation, GET/POST routing, parameterized routes, validation pipeline, missing handler error
- Files changed: packages/server-express/src/index.ts, packages/server-express/src/index.test.ts, packages/server-express/package.json, packages/server-express/tsconfig.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Express 5.x works with @types/express ^5.0.0 — types include Application, Request, Response, Express
  - Express route methods are lowercase (app.get, app.post) vs Fastify which accepts uppercase method in app.route()
  - Express uses req.query (already parsed) vs Hono which needs manual URL parsing
  - Express needs express.json() middleware explicitly for body parsing unlike Fastify which handles it natively
  - Use node:http createServer(app) for Express listen() to get Server instance for port 0 auto-assignment
  - Server adapter pattern is highly consistent across Fastify/Hono/Express — collectRoutes, runValidators, serializeResponse are identical
---
