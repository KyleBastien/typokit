# Ralph Progress Log
Started: 03/08/2026 23:22:45

## Codebase Patterns
- Benchmarks package uses Nx auto-infer (no project.json needed) — consistent with all packages
- Benchmarks project.json defines custom targets (bench, bench-ci, etc.) with nx:run-commands executor
- pnpm-workspace.yaml `packages/*` glob auto-detects new packages
- Raw baseline apps use hand-written routing (no framework) — same 5 endpoints as TypoKit apps
- ESLint enforces LF line endings — use `eslint --fix` to auto-convert CRLF on Windows
- ESLint forbids `import()` type annotations — use explicit `import type` at top of file
- Server adapter apps follow shared-routes pattern per platform (shared-routes.ts, shared-routes-bun.ts, shared-routes-deno.ts)
- Root .gitignore has `target/` for Rust and `.typokit/` for generated files
- Axum 0.8 path params use `{id}` syntax (not `:id`)
- For rusqlite in Axum, use `Arc<Mutex<Connection>>` + `spawn_blocking` since Connection is Send but not Sync
- Build target (tsc -p tsconfig.lib.json) fails on Bun/Deno files; typecheck (tsc --noEmit) is the real quality gate
- Competitor apps use each framework's recommended validation (JSON Schema for Fastify, TypeBox for Elysia, Zod for tRPC, manual for others)
- NestJS requires experimentalDecorators + emitDecoratorMetadata in tsconfig (enabled in benchmarks tsconfig.json)
- AdonisJS v6 can't work in single-file mode; benchmark uses AdonisJS-style HTTP context/middleware pattern
- tRPC standalone adapter needs URL rewriting for REST-style /db/:id → /db?input={"id":X}
- Astro components can't use `import.meta.dirname` — use `fileURLToPath` + `dirname()` instead
- Starlight sidebar fails at build time if referenced slugs don't exist — always create stub pages

---

## 2026-03-09T07:05 - US-005
- What was implemented: TypoKit Deno benchmark apps for all 4 server adapters (native, fastify, hono, express)
- Files changed:
  - packages/benchmarks/src/deno-sqlite.d.ts (new: type declarations for @db/sqlite module)
  - packages/benchmarks/src/apps/shared-routes-deno.ts (new: Deno-specific resource builder using @db/sqlite)
  - packages/benchmarks/src/apps/typokit-deno-native.ts (new: native server adapter on Deno)
  - packages/benchmarks/src/apps/typokit-deno-fastify.ts (new: Fastify adapter on Deno)
  - packages/benchmarks/src/apps/typokit-deno-hono.ts (new: Hono adapter on Deno)
  - packages/benchmarks/src/apps/typokit-deno-express.ts (new: Express adapter on Deno)
  - packages/benchmarks/src/apps/index.ts (added Deno app exports)
  - packages/benchmarks/package.json (added @typokit/platform-deno dep)
  - packages/benchmarks/tsconfig.lib.json (Nx synced platform-deno reference)
  - pnpm-lock.yaml (updated)
- **Learnings for future iterations:**
  - @db/sqlite API returns undefined (not null) when no row found - same as better-sqlite3
  - Deno SQLite type declarations follow same pattern as bun:sqlite - minimal .d.ts with Database/Statement classes
  - Nx sync auto-detected the new @typokit/platform-deno dependency and added the tsconfig reference automatically
  - The Deno apps follow identical pattern to Bun apps - only the DB layer (shared-routes-deno.ts) differs; server adapters work via Deno Node compat
---

## 2026-03-09T07:08 - US-001
- What was implemented: Already fully implemented by prior iteration - verified all acceptance criteria pass
- Files: No changes needed - all files exist (package.json, types.ts, shared/, fixtures/)
- **Learnings for future iterations:**
  - Benchmarks package uses Nx auto-infer (no project.json) consistent with all other packages
  - pnpm-workspace.yaml uses packages/* glob, so new packages are auto-detected
  - All quality checks (typecheck, lint) pass for the existing scaffolding
---

## 2026-03-09T07:09 - US-002, US-003, US-004
- What was implemented: Already fully implemented by prior iterations - verified all exist and typecheck/lint pass
- Files: No changes - bombardier.ts, all Node/Bun/Deno app files, shared-routes files all present
- **Learnings for future iterations:**
  - All server adapter apps (node/bun/deno × native/fastify/hono/express) follow shared-routes pattern
  - Each platform has its own shared-routes variant (shared-routes.ts, shared-routes-bun.ts, shared-routes-deno.ts)
---

## 2026-03-09T07:12 - US-006
- What was implemented: Rust/Axum benchmark app with 5 endpoints (json, validate, db, middleware, startup)
- Files changed:
  - packages/benchmarks/src/apps/typokit-rust-axum/Cargo.toml (new)
  - packages/benchmarks/src/apps/typokit-rust-axum/Cargo.lock (new)
  - packages/benchmarks/src/apps/typokit-rust-axum/src/main.rs (new: tokio entrypoint with --port/PORT support)
  - packages/benchmarks/src/apps/typokit-rust-axum/src/models.rs (new: BenchmarkResponseShape, CreateBenchmarkItemBody, validation)
  - packages/benchmarks/src/apps/typokit-rust-axum/src/db.rs (new: rusqlite read-only SQLite access)
  - packages/benchmarks/src/apps/typokit-rust-axum/src/handlers.rs (new: 5 endpoint handlers)
  - packages/benchmarks/src/apps/typokit-rust-axum/src/middleware.rs (new: noop middleware for chain overhead testing)
- **Learnings for future iterations:**
  - Axum 0.8 uses {id} path syntax (not :id); Cargo edition 2021 works reliably
  - rusqlite::Connection is Send but not Sync; use Arc<Mutex<Connection>> with spawn_blocking for Axum State
  - Root .gitignore already has target/ so Cargo build artifacts are excluded
  - The alidator crate was NOT needed — manual validation is simpler and avoids the derive dependency
  - Use OnceLock for static startup time tracking instead of lazy_static
---

## 2026-03-09T07:16 - US-007
- What was implemented: Raw baseline benchmark apps for Node.js, Bun, Deno, and Axum - zero framework dependencies
- Files changed:
  - packages/benchmarks/src/apps/raw-node.ts (new: raw http.createServer with 5 endpoints)
  - packages/benchmarks/src/apps/raw-bun.ts (new: raw Bun.serve() with 5 endpoints)
  - packages/benchmarks/src/apps/raw-deno.ts (new: raw Deno.serve() with 5 endpoints)
  - packages/benchmarks/src/apps/raw-axum/Cargo.toml (new: identical deps to typokit-rust-axum)
  - packages/benchmarks/src/apps/raw-axum/src/main.rs (new: tokio entrypoint)
  - packages/benchmarks/src/apps/raw-axum/src/models.rs (new: same models + validation)
  - packages/benchmarks/src/apps/raw-axum/src/db.rs (new: rusqlite read-only)
  - packages/benchmarks/src/apps/raw-axum/src/handlers.rs (new: 5 endpoint handlers)
  - packages/benchmarks/src/apps/raw-axum/src/middleware.rs (new: noop middleware)
  - packages/benchmarks/src/apps/index.ts (added raw app exports)
- **Learnings for future iterations:**
  - ESLint enforces LF line endings via prettier - files created on Windows need eslint --fix to strip CRLF
  - ESLint consistent-type-imports forbids import() type annotations - use explicit import type at file top
  - Raw apps duplicate validation logic from shared-routes.ts by design (no framework code allowed)
  - Raw Axum structure is identical to typokit-rust-axum - only the package name and log message differ
  - For the no-op middleware simulation in raw TS apps, a simple for-loop is sufficient (vs Axum layer stacking)
---

## 2026-03-09T07:23 - US-008
- What was implemented: Competitor framework benchmark apps for Express 5, Fastify 5, Hono 4, Koa 2, and Elysia (Bun)
- Files changed:
  - packages/benchmarks/src/apps/competitor-express.ts (new: standalone Express with 5 endpoints)
  - packages/benchmarks/src/apps/competitor-fastify.ts (new: standalone Fastify with JSON Schema validation)
  - packages/benchmarks/src/apps/competitor-hono.ts (new: standalone Hono + @hono/node-server)
  - packages/benchmarks/src/apps/competitor-koa.ts (new: standalone Koa + @koa/router + @koa/bodyparser)
  - packages/benchmarks/src/apps/competitor-elysia.ts (new: standalone Elysia with TypeBox validation, bun:sqlite)
  - packages/benchmarks/src/apps/index.ts (added competitor app exports)
  - packages/benchmarks/package.json (added koa, elysia, express, fastify, hono, @hono/node-server as direct deps)
  - pnpm-lock.yaml (updated)
- **Learnings for future iterations:**
  - Fastify preHandler hooks require mutable arrays (Array<>), not ReadonlyArray
  - Fastify recommended validation is JSON Schema (built-in Ajv)
  - Elysia uses t from elysia (TypeBox wrapper) for built-in validation schemas
  - Hono on Node.js requires @hono/node-server with serve() function
  - Koa requires @koa/router for routing and @koa/bodyparser for JSON body parsing
  - Build target (tsc -p tsconfig.lib.json) fails on Bun/Deno files but typecheck (tsc --noEmit) passes - typecheck is the real quality gate
---

## 2026-03-09T07:32 - US-009
- What was implemented: 4 new competitor framework benchmark apps (tRPC, NestJS, H3/Nitro, AdonisJS)
- Files changed:
  - packages/benchmarks/src/apps/competitor-trpc.ts (new: tRPC 10 with standalone HTTP adapter, Zod validation, URL rewriting for /db/:id)
  - packages/benchmarks/src/apps/competitor-nestjs.ts (new: NestJS 10 with Express platform, decorators, hand-written validation)
  - packages/benchmarks/src/apps/competitor-h3.ts (new: H3/Nitro with createApp/createRouter, onRequest hooks for middleware)
  - packages/benchmarks/src/apps/competitor-adonis.ts (new: AdonisJS-style HTTP server with context creation, middleware chain, router)
  - packages/benchmarks/src/apps/index.ts (added 4 new competitor app exports)
  - packages/benchmarks/package.json (added h3, @trpc/server, zod, @nestjs/*, reflect-metadata dependencies)
  - packages/benchmarks/tsconfig.json (added experimentalDecorators + emitDecoratorMetadata for NestJS)
  - pnpm-lock.yaml (updated)
- **Learnings for future iterations:**
  - NestJS requires experimentalDecorators and emitDecoratorMetadata in tsconfig; added only to benchmarks tsconfig.json
  - AdonisJS v6 cannot work in single-file mode; benchmark uses AdonisJS-style HTTP context/middleware/router pattern
  - tRPC standalone adapter serves at /<name> paths; URL rewriting maps /db/:id to /db?input={"id":X}
  - H3 defineEventHandler supports onRequest hooks array for route-specific middleware
  - reflect-metadata must be imported before any NestJS decorators
---

## 2026-03-09T07:48 - US-010
- What was implemented: Benchmark runner core with full orchestration and Nx CLI targets
- Files changed:
  - packages/benchmarks/src/runner.ts (new: main runner with app registry, CLI arg parsing, orchestration, summary table, results file output)
  - packages/benchmarks/src/subprocess-entry.ts (new: generic subprocess launcher for Bun/Deno apps)
  - packages/benchmarks/src/bombardier.ts (modified: added method/body/headers to BombardierRunConfig for POST scenario support)
  - packages/benchmarks/project.json (new: Nx targets — bench, bench-ci, bench-baseline, bench-info, bench-reproduce)
  - packages/benchmarks/results/.gitkeep (new: results output directory)
  - packages/benchmarks/package.json (added tsx devDependency)
  - package.json (added root bench scripts)
  - pnpm-lock.yaml (updated)
- **Learnings for future iterations:**
  - project.json with nx:run-commands executor works alongside Nx auto-infer for custom targets
  - tsx is needed as devDep for running TypeScript benchmark scripts directly (no build step required)
  - Bombardier supports POST via `-m POST -b <body> -H "header: value"` flags; execFile avoids shell escaping issues
  - Cross-runtime subprocess management uses subprocess-entry.ts which dynamically imports app modules and reports port via stdout
  - Rust binary names come from Cargo.toml `[package] name` field, not the directory name (e.g., `typokit-benchmark-axum` not `typokit-rust-axum`)
  - Direct-mode apps (Node.js) use dynamic imports `await import("./apps/<name>.ts")` — works with tsx
---
## 2026-03-09T08:03 - US-011
- What was implemented: Results aggregation module (results.ts) with summary table, cumulative latest.json merge, and bombardier version in system info
- Files changed:
  - packages/benchmarks/src/results.ts (new: formatSummaryTable, printSummaryTable, writeTimestampedResults, mergeLatestResults, averageSuiteRuns, getBombardierVersion)
  - packages/benchmarks/src/runner.ts (refactored: removed old printSummaryTable/writeResults/padLeft/padRight, imports from results.ts, async collectSystemInfo with bombardier version, main() calls mergeLatestResults)
  - packages/benchmarks/src/types.ts (added bombardierVersion to SystemInfo)
- **Learnings for future iterations:**
  - Summary tables are grouped by scenario with Rank, vs. Fastest (%) computed from highest req/s in each group
  - Cumulative merge uses composite key: framework|platform|server|scenario -- new results overwrite matching entries, others preserved
  - Results files now use ResultsFile wrapper { version, generatedAt, results } for forward compatibility; legacy raw arrays also supported on read
  - getBombardierVersion() runs `bombardier --version` and parses the version string
---
## 2026-03-09T08:09 - US-012
- What was implemented: System info collection module (system-info.ts) with comprehensive runtime detection, JSON bench-info output, and full bench-reproduce instructions
- Files changed:
  - packages/benchmarks/src/system-info.ts (new: collectSystemInfo, formatSystemInfoJson, formatReproduceInstructions with Bun/Deno/Rust detection)
  - packages/benchmarks/src/runner.ts (refactored: removed inline collectSystemInfo, imports from system-info.ts, printSystemInfo outputs JSON, printReproduceInfo uses full step-by-step instructions)
- **Learnings for future iterations:**
  - `bun --version` outputs just the version number (e.g., "1.1.12"), while `deno --version` and `rustc --version` need regex parsing
  - ESLint CRLF fix pattern applies to all new .ts files created on Windows — always run eslint --fix after creating files
  - The printSystemInfo function now outputs parseable JSON; consumer scripts can pipe through `jq` for specific fields
  - The reproduce instructions include both exact reproduction (with config from latest results) and generic defaults when no results exist
---

## 2026-03-09T08:15 - US-013
- What was implemented: Validation overhead isolation variants for all 13 TypoKit benchmark apps
  - `/validate-passthrough` endpoint (no validation, body passthrough)
  - `/validate-handwritten` endpoint (inline if/typeof validation, bypasses TypoKit validator framework)
  - Runner scenarios `validate-passthrough` and `validate-handwritten` (TypoKit-only, skipped for raw/competitor apps)
  - Validation Overhead Analysis table in summary output showing vs-passthrough and vs-handwritten deltas
  - `validationAnalysis` field in results JSON (timestamped + latest.json)
  - `ValidationAnalysisEntry` type in types.ts
- Files changed:
  - packages/benchmarks/src/apps/shared-routes.ts (new route table entries, handwrittenValidate function, passthrough/handwritten handlers)
  - packages/benchmarks/src/apps/shared-routes-bun.ts (passthrough/handwritten handlers, re-export handwrittenValidate)
  - packages/benchmarks/src/apps/shared-routes-deno.ts (passthrough/handwritten handlers, re-export handwrittenValidate)
  - packages/benchmarks/src/apps/index.ts (added handwrittenValidate export)
  - packages/benchmarks/src/apps/typokit-rust-axum/src/handlers.rs (post_validate_passthrough, post_validate_handwritten handlers)
  - packages/benchmarks/src/apps/typokit-rust-axum/src/main.rs (registered new routes)
  - packages/benchmarks/src/runner.ts (Scenario type expanded, SCENARIO_DEFS, TYPOKIT_ONLY_SCENARIOS set, filtering in run loop)
  - packages/benchmarks/src/types.ts (ValidationAnalysisEntry interface)
  - packages/benchmarks/src/results.ts (computeValidationAnalysis, formatValidationAnalysis, updated writeTimestampedResults and mergeLatestResults)
- **Learnings for future iterations:**
  - The handwrittenValidate function is exported from shared-routes.ts and reused by bun/deno variants to avoid duplication
  - TYPOKIT_ONLY_SCENARIOS set in runner.ts controls which scenarios are skipped for non-TypoKit apps (checked via `app.framework !== "typokit"`)
  - Validation analysis is computed post-hoc from the three validate scenario results grouped by framework|platform|server
  - Passthrough endpoint has NO validators in the route table — this is the key to measuring TypoKit validator framework overhead
  - Handwritten validation bypasses ValidatorMap entirely — validation happens inline in the handler, measuring framework dispatch overhead
---

## 2026-03-09T08:24 - US-014
- What was implemented: Non-HTTP serialization microbenchmark comparing 3 serializers
  - JSON.stringify (native baseline)
  - fast-json-stringify (schema-compiled, popular npm package)
  - TypoKit compiled serializer (direct property access, build-time generated)
- Files changed:
  - packages/benchmarks/src/microbench/serialization.ts (new: microbenchmark harness with configurable iterations, warmup, timing, summary table)
  - packages/benchmarks/src/types.ts (added MicrobenchmarkResult interface)
  - packages/benchmarks/src/results.ts (added microbenchmarks key to ResultsFile, updated writeTimestampedResults and mergeLatestResults)
  - packages/benchmarks/src/runner.ts (added serialization to Scenario type, split HttpScenario vs Scenario, integrated microbenchmark in main())
  - packages/benchmarks/package.json (added fast-json-stringify devDependency)
  - pnpm-lock.yaml (updated)
- **Learnings for future iterations:**
  - The Scenario type needed splitting into HttpScenario (used by SCENARIO_DEFS/benchmarkScenario) and Scenario (union of HttpScenario | serialization) to avoid type errors with Record<Scenario, ScenarioDef>
  - fast-json-stringify types work with tsc --noEmit (typecheck) but may show TS2769 under tsc -p tsconfig.lib.json (build) -- same pre-existing build failure pattern as Bun/Deno files
  - performance.now() returns milliseconds; multiply by 1_000_000 for nanosecond precision in microbenchmarks
  - Float64Array is more efficient than number[] for large timing arrays (1M iterations)
  - Microbenchmark results use a separate merge key (name|serializer) distinct from HTTP benchmark composite key (framework|platform|server|scenario)
---

## 2026-03-09T08:33 - US-015
- What was implemented: GitHub Actions benchmark workflow with PR regression detection and main branch full suite
- Files changed:
  - .github/workflows/benchmark.yml (new: CI workflow with check-affected, bench-pr, and bench-main jobs)
  - packages/benchmarks/src/compare-baseline.ts (new: baseline comparison script with >10% regression threshold, markdown output)
  - packages/benchmarks/project.json (updated: bench-baseline target now clears latest.json, runs bench-ci filter, copies to baseline.json)
- **Learnings for future iterations:**
  - The BenchmarkResult.framework field stores the full app name (e.g., "typokit-node-native"), NOT the framework category ("typokit")
  - ESLint CRLF fix is needed for every new .ts file created on Windows — consistent with previous iterations
  - actions-rust-lang/setup-rust-toolchain@v1 is the correct action for Rust setup in CI (different from dtolnay/rust-toolchain@stable used in ci.yml)
  - The docs.yml workflow already supports workflow_dispatch, so bench-main can trigger docs deploy via createWorkflowDispatch API
  - nx show projects --affected is the correct way to check if a specific package is affected in CI
  - PR comment uses "## Benchmark Results vs Baseline" as marker to find/update existing comments
---

## 2026-03-09T08:41 - US-016
- What was implemented: Docs site Benchmarks page with Chart.js interactive charts
  - Performance sidebar section added to astro.config.mjs
  - BenchmarkCharts.astro component: reads latest.json at build time, falls back to sample data
  - 4 bar charts (JSON, Validate, DB req/s on log scale; Startup on linear)
  - Platform × Server heatmap (13 TypoKit combinations, Rust/Axum distinguished in orange)
  - Latency percentile comparison table (top 15 frameworks)
  - Consistent color scheme: purple=TypoKit, orange=Rust, gray=raw, named colors for competitors
  - Methodology stub page (placeholder for US-017)
  - chart.js added to docs package.json, CDN script for client-side rendering
- Files changed:
  - packages/docs/astro.config.mjs (added Performance sidebar section, Chart.js CDN script tag)
  - packages/docs/package.json (added chart.js dependency)
  - packages/docs/src/components/BenchmarkCharts.astro (new: full chart component with build-time data loading)
  - packages/docs/src/content/docs/performance/benchmarks.mdx (new: Benchmarks overview page)
  - packages/docs/src/content/docs/performance/methodology.mdx (new: methodology stub for US-017)
  - pnpm-lock.yaml (updated)
- **Learnings for future iterations:**
  - Astro components can't use `import.meta.dirname` — use `fileURLToPath(import.meta.url)` + `dirname()` instead
  - Chart.js is loaded via CDN in Starlight head config; Astro `<script define:vars>` passes serialized data to client
  - Starlight sidebar fails at build time if referenced slugs don't exist — create stub pages for all sidebar entries
  - Sample data generation at build time allows docs to build even before benchmarks are run
  - The docs build takes ~34s and validates all sidebar entries, content, and component imports
---

## 2026-03-09T08:49 - US-017
- What was implemented: Full Methodology documentation page for the benchmark suite
  - CI runner hardware specs section
  - Bombardier settings (default: 100 conn/30s/5s warmup/3 runs, CI: 50 conn/10s/1 run)
  - All 8 scenarios documented (json, validate, validate-passthrough, validate-handwritten, db, middleware, startup, serialization)
  - Complete framework list with 25 apps (13 TypoKit, 4 raw, 8 competitor) with versions and links
  - Local reproduction instructions with prerequisites and all CLI commands
  - Nx affected scoping explanation (partial runs, cumulative latest.json merge, skip conditions)
  - CI regression detection documentation (baseline.json, 10% threshold, PR comments, main branch workflow)
  - Results format documentation with example JSON
  - Methodology sidebar entry already existed from US-016
- Files changed:
  - packages/docs/src/content/docs/performance/methodology.mdx (replaced stub with full documentation)
  - scripts/ralph/prd.json (US-017 passes set to true)
- **Learnings for future iterations:**
  - The methodology.mdx stub was already created by US-016 with sidebar entry — only content needed replacing
  - Astro/Starlight .mdx files support standard markdown tables and code blocks without special imports
  - LinkCard component from @astrojs/starlight/components is useful for cross-page navigation
---
