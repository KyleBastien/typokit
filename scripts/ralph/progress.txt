# Ralph Progress Log
Started: 03/02/2026 09:17:20
---

## Codebase Patterns
- Monorepo uses pnpm workspaces with packages in `packages/*`
- Package naming: `@typokit/{package-name}`, version `0.1.0`, all `"private": true`
- Nx handles build orchestration: `nx run-many --target={build|test|lint|typecheck}`
- ESM modules throughout (`"type": "module"`)
- Build scripts: each package uses `pnpm approve-builds` for native deps (esbuild, sharp)
- `.astro/` is gitignored; generated types are auto-created by `astro sync` / `astro build`
- Starlight social config uses object format `{ github: "url" }`, NOT array format
- Docs package needs `project.json` to override Nx target defaults (uses astro commands, not tsc)
- Starlight sidebar slugs reference paths under `src/content/docs/` without extension; `index.mdx` uses directory path as slug
- GitHub Pages for project repos need `base: "/repo-name"` in astro.config.mjs
- CI workflow uses `pnpm nx build docs` to build the docs package (not `nx run docs:build`)
- starlight-typedoc uses `entryPointStrategy: "packages"` for monorepo; each package dir is an entry point
- Generated API reference files go to `api-reference/generated/` and are gitignored (regenerated on build)
- Use `typeDocSidebarGroup` from starlight-typedoc to insert auto-generated sidebar; replaces manual API Reference section
- Set `errorOnEmptyDocumentation: false` since some packages may lack full JSDoc coverage
- Starlight UI components (Tabs, Steps, Aside, etc.) import from `@astrojs/starlight/components` in MDX files
- Internal doc links require the `base` prefix for GitHub Pages (e.g., `/typokit/path/to/page/`)

## 2026-03-02T17:23 - US-001
- Initialized Starlight docs package at packages/docs/
- Created: package.json, astro.config.mjs, tsconfig.json, src/content.config.ts, src/content/docs/index.mdx
- Files changed: packages/docs/ (new), .gitignore, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Starlight `social` config changed to object format in recent versions (not array)
  - `@astrojs/check` + `typescript` must be installed separately for `astro check`
  - `.astro/` dir contains generated types; gitignored, regenerated on build/sync
  - Pagefind search is automatic during build, no config needed
  - `site` option in astro.config.mjs is required for sitemap generation
---

## 2026-03-02T17:28 - US-003
- Configured full sidebar navigation in astro.config.mjs with 9 collapsible sections
- Created 26 placeholder .mdx stub files for all planned docs pages
- Sections: Getting Started, Core Concepts, Guides, CLI Reference, API Reference, Architecture, Contributing, AI Agents, Blog
- Files changed: packages/docs/astro.config.mjs, 26 new .mdx files under packages/docs/src/content/docs/
- **Learnings for future iterations:**
  - Starlight sidebar uses `slug` property referencing paths under `src/content/docs/` (without extension)
  - `collapsed: true` makes sidebar sections collapsible (collapsed by default); `collapsed: false` for expanded
  - Starlight active page highlighting and mobile hamburger menu work automatically — no extra config needed
  - Slug for `index.mdx` files uses the directory path (e.g., `api-reference` for `api-reference/index.mdx`)
  - Empty slug `""` references the root `index.mdx`
---

## 2026-03-02T17:25 - US-002
- Created `packages/docs/project.json` with Nx targets overriding defaults
- Targets: build (astro build), serve (astro dev), typecheck (astro check), lint/test (no-ops)
- Files changed: packages/docs/project.json (new)
- **Learnings for future iterations:**
  - Nx target defaults in nx.json use `tsc`, but Astro packages need `astro build`/`astro check`
  - Override targets via `project.json` in the package root, not via package.json nx config
  - `nx serve docs` starts dev server on localhost:4321 automatically
  - Lint and test targets need no-op overrides to prevent default tsc/rstest/eslint from running
---

## 2026-03-02T17:31 - US-004
- Created `.github/workflows/docs.yml` for GitHub Pages deployment
- Workflow triggers on push to main (with paths filter) and workflow_dispatch
- Paths filter: `packages/docs/**`, `packages/*/src/**`, `typokit-arch.md`
- Installs deps with pnpm, builds with `pnpm nx build docs`
- Deploys `packages/docs/dist/` using `actions/upload-pages-artifact@v3` + `actions/deploy-pages@v4`
- Updated `packages/docs/astro.config.mjs` with `base: "/typokit"` for GitHub Pages project site
- Files changed: `.github/workflows/docs.yml` (new), `packages/docs/astro.config.mjs`
- **Learnings for future iterations:**
  - GitHub Pages project repos (not `<user>.github.io`) need `base: "/repo-name"` in astro config
  - Use `concurrency` group with `cancel-in-progress: false` for Pages deployments to avoid interrupted deploys
  - Pages deployment requires `permissions: pages: write, id-token: write, contents: read`
  - The `actions/configure-pages` step is optional when using `upload-pages-artifact` directly
---

## 2026-03-02T17:34 - US-005
- Installed starlight-typedoc, typedoc, and typedoc-plugin-markdown in packages/docs
- Configured starlight-typedoc plugin in astro.config.mjs with all 24 publishable @typokit/* packages
- Used `entryPointStrategy: "packages"` for per-package API reference sections
- Replaced manual "API Reference" sidebar item with `typeDocSidebarGroup` auto-generated sidebar
- Added `api-reference/generated/` to .gitignore (regenerated on every build)
- Set `errorOnEmptyDocumentation: false` for graceful handling of packages with minimal JSDoc
- Updated api-reference/index.mdx stub with helpful description
- Files changed: packages/docs/astro.config.mjs, packages/docs/package.json, .gitignore, packages/docs/src/content/docs/api-reference/index.mdx, pnpm-lock.yaml, pnpm-workspace.yaml
- **Learnings for future iterations:**
  - starlight-typedoc requires `typedoc` and `typedoc-plugin-markdown` as peer deps (auto-installed by pnpm)
  - For monorepos, use `entryPointStrategy: "packages"` in the `typeDoc` config; each entry point is a package directory containing package.json + tsconfig.json
  - `typeDocSidebarGroup` is exported from `starlight-typedoc` and must be placed in the sidebar array to show generated API docs
  - TypeDoc warnings about types "not included in the documentation" are normal for internal/unexported types referenced by public APIs
  - Generated markdown files go to `src/content/docs/{output}/` where `output` is configured in the plugin
  - The `tsconfig` option should point to root tsconfig.json for proper path resolution across packages
  - Build generates 323 markdown files and 351 total pages with TypeDoc integration enabled
---

## 2026-03-02T17:41 - US-006
- Wrote comprehensive quickstart guide at packages/docs/src/content/docs/getting-started/quickstart.mdx
- Guide covers: prerequisites (Node.js 24+, pnpm/npm/yarn), scaffold init, project structure overview, defining types, creating routes with contracts+handlers, registering routes, starting dev server, testing with curl
- Uses Starlight components: Tabs, TabItem, Steps, Aside for polished UX
- Includes terminal command blocks with expected output at each step
- Links to Core Concepts pages (schema-first-types, routing-and-handlers, middleware-and-context, error-handling) and guides
- Files changed: packages/docs/src/content/docs/getting-started/quickstart.mdx
- **Learnings for future iterations:**
  - Starlight `Tabs`, `TabItem`, `Steps`, and `Aside` components are imported from `@astrojs/starlight/components`
  - MDX files support JSX components inline with markdown — no special config needed
  - Internal links in Starlight docs need the `base` prefix (e.g., `/typokit/core-concepts/...`) for GitHub Pages deployment
  - Typecheck and build both pass with MDX component imports; no additional dependencies needed
---

## 2026-03-02T17:49 - US-007
- Wrote comprehensive project structure guide at packages/docs/src/content/docs/getting-started/project-structure.mdx
- Covers: 4-package monorepo layout (@app/schema, @app/server, @app/db, @app/client), .typokit/ generated directory with all subdirectories explained, typokit.config.ts options, auto-generated vs user-maintained file table
- Uses Starlight components: Aside (tip, caution), Tabs/TabItem for code examples
- Includes file tree diagrams, dependency flow diagram, and configuration examples
- Files changed: packages/docs/src/content/docs/getting-started/project-structure.mdx
- **Learnings for future iterations:**
  - The explore agent is very effective for gathering codebase architecture info from typokit-arch.md and example packages
  - Project structure docs should clearly mark which files are auto-generated vs user-maintained — this is a key TypoKit concept
  - The .typokit/ directory has 5 subdirectories: validators/, routes/, schemas/, tests/, client/
  - typokit.config.ts uses defineConfig() from @typokit/cli with typeFiles, routeFiles, outputDir, distDir, compiler options
  - Build produces 351 pages total (same as before — no new pages since this replaced a stub)
---

## 2026-03-02T17:54 - US-008
- Wrote comprehensive Core Concept page for Schema-First Types at packages/docs/src/content/docs/core-concepts/schema-first-types.mdx
- Covers: philosophy (single source of truth), complete entity type example with all JSDoc tags, JSDoc tag reference table, tag-to-output mapping, derived types (Omit/Partial/Pick), build artifacts (validators, DB schema, OpenAPI, client, tests, diff reports), "What NOT to Do" section
- Uses Starlight components: Aside (tip, note, caution), Tabs/TabItem for database adapter examples
- Includes cross-links to routing-and-handlers, middleware-and-context, error-handling, and building-first-api
- Files changed: packages/docs/src/content/docs/core-concepts/schema-first-types.mdx
- **Learnings for future iterations:**
  - The explore agent provides excellent summaries of the architecture from typokit-arch.md and package source files — use it for gathering context before writing docs
  - Schema-first types use JSDoc tags at both interface level (@table) and field level (@id, @generated, etc.)
  - Derived types follow consistent patterns: Omit for create inputs, Partial for update inputs, Pick for summaries, Omit for public outputs
  - The build pipeline has 6 artifact categories: validators, DB schema, OpenAPI, client, tests, diff reports
  - 351 pages total after build (same count — replaced a stub page)
---

## 2026-03-02T17:55 - US-009
- Wrote comprehensive Core Concept page for Routing and Handlers at packages/docs/src/content/docs/core-concepts/routing-and-handlers.mdx
- Covers: RouteContract<TParams, TQuery, TBody, TResponse> with full examples, defineHandlers<TRoutes>() with typed handler implementation, file convention (contracts.ts, handlers.ts, middleware.ts), explicit route registration in app.ts, automatic validation of params/query/body, full end-to-end flow example
- Uses Starlight components: Aside (tip, note, caution), Tabs/TabItem not needed for this page
- Cross-links to schema-first-types, middleware-and-context, error-handling, building-first-api
- Files changed: packages/docs/src/content/docs/core-concepts/routing-and-handlers.mdx
- **Learnings for future iterations:**
  - The explore agent provides excellent codebase summaries including actual handler implementations from example packages
  - RouteContract has 4 type params (TParams, TQuery, TBody, TResponse) all defaulting to void
  - defineHandlers is a pure type-validation pass-through at runtime — returns handlers unchanged
  - HandlerInput provides params, query, body, and ctx — all typed from the contract
  - Route registration uses RouteGroup with prefix, handlers, and optional middleware array
  - ctx.fail() is the standard error pattern in handlers (covered in detail in Error Handling page)
  - 351 pages total after build (same count — replaced a stub page)
---

## 2026-03-02T18:04 - US-010
- Wrote comprehensive Core Concept page for Middleware and Context at packages/docs/src/content/docs/core-concepts/middleware-and-context.mdx
- Covers: RequestContext interface (ctx.fail, ctx.log, ctx.services, ctx.requestId), defineMiddleware<TAdded>() with full auth example, context type narrowing mechanism, executeMiddlewareChain internals, priority ordering (lower runs first), short-circuiting via ctx.fail(), global vs route-group registration, typed vs framework-native middleware distinction, request processing order diagram, common patterns (logging, tenant resolution, rate limiting)
- Uses Starlight components: Aside (tip, note, caution)
- Cross-links to routing-and-handlers, error-handling, server-adapters, building-first-api
- Files changed: packages/docs/src/content/docs/core-concepts/middleware-and-context.mdx
- **Learnings for future iterations:**
  - The middleware-and-context.mdx file is the correct name (sidebar slug: core-concepts/middleware-and-context), not middleware.mdx as AC states
  - defineMiddleware generic TAdded type must include index signature [key: string]: unknown for context spread compatibility
  - executeMiddlewareChain sorts by priority (lower first, default 0), then accumulates returned objects via spread
  - Middleware short-circuits by calling ctx.fail() which throws AppError — remaining middleware and handler don't execute
  - Framework-native middleware (CORS, compression) runs BEFORE TypoKit normalization; typed middleware runs AFTER
  - 351 pages total after build (same count — replaced a stub page)
---

## 2026-03-02T18:08 - US-011
- Wrote comprehensive Core Concept page for Error Handling at packages/docs/src/content/docs/core-concepts/error-handling.mdx
- Covers: thrown errors philosophy (vs Result types), AppError class hierarchy (NotFoundError, ValidationError, UnauthorizedError, ForbiddenError, ConflictError), ctx.fail() usage as syntactic sugar, built-in error middleware (createErrorMiddleware), Typia validation error auto-formatting, dev mode vs production mode differences (stack trace exposure), structured error JSON for AI agents (traceId, route, phase, failures, sourceFile, schemaFile, relatedTests), best practices
- Uses Starlight components: Aside (tip, note, caution)
- Cross-links to middleware-and-context, routing-and-handlers, observability, building-first-api
- Files changed: packages/docs/src/content/docs/core-concepts/error-handling.mdx
- **Learnings for future iterations:**
  - AppError class is in @typokit/errors package with 5 subclasses: NotFoundError (404), ValidationError (400), UnauthorizedError (401), ForbiddenError (403), ConflictError (409)
  - ctx.fail() creates the appropriate AppError subclass based on status code and throws it — return type is `never`
  - createErrorMiddleware() in @typokit/core handles 3 error categories: AppError, Typia validation errors, unknown errors
  - Dev mode exposes stack traces and error messages; production returns generic "Internal Server Error" and logs full details server-side
  - Extended error format includes sourceFile, schemaFile, relatedTests fields specifically designed for AI agent consumption
  - 351 pages total after build (same count — replaced a stub page)
---
## 2026-03-02T18:22 - US-015
- Wrote comprehensive Core Concept page for Testing at packages/docs/src/content/docs/core-concepts/testing.mdx
- Covers: schema-first testing philosophy, auto-generated contract tests (3 coverage levels: valid input, missing fields, invalid formats), createTestClient with typed HTTP methods, createIntegrationSuite with in-memory database and seed fixtures, createFactory with build/buildMany/buildInvalid methods, toMatchSchema custom matcher, CLI commands (typokit test, test:contracts, test:integration), CI consistency guarantees (idempotent generation, isolated tests, flaky detection, deterministic factories), full integration test example
- Uses Starlight components: Aside (tip, note, caution)
- Cross-links to building-first-api, error-handling, generate-migrate-test CLI reference
- Files changed: packages/docs/src/content/docs/core-concepts/testing.mdx
- **Learnings for future iterations:**
  - Contract test generation groups routes by first path segment and outputs one test file per group to __generated__/
  - createFactory uses seeded Mulberry32 PRNG for deterministic data generation respecting all JSDoc constraints
  - createIntegrationSuite provides both suite.client (HTTP) and suite.db (direct DB access) for full-stack testing
  - Schema matcher (toMatchSchema) works with Jest, Vitest, and Rstest — framework-agnostic
  - CLI auto-detects test runner from config files; defaults to Vitest if none found
  - 351 pages total after build (same count — replaced a stub page)
---

## 2026-03-02T18:10 - US-012
- Wrote comprehensive Core Concept page for Server Adapters at packages/docs/src/content/docs/core-concepts/server-adapters.mdx
- Covers: philosophy (TypoKit is not an HTTP server), ownership boundary table (TypoKit vs adapter responsibilities), request processing order diagram (10-step flow), ServerAdapter interface definition, all 4 adapters (Native, Fastify, Hono, Express) with full usage examples, when to use native vs bring-your-own decision guide, platform adapters as orthogonal axis with compatibility matrix, getNativeServer() escape hatch
- Uses Starlight components: Aside (tip, note, caution)
- Cross-links to custom-server-adapter guide, middleware-and-context, error-handling, building-first-api
- Files changed: packages/docs/src/content/docs/core-concepts/server-adapters.mdx
- **Learnings for future iterations:**
  - ServerAdapter interface is defined in @typokit/core at packages/core/src/adapters/server.ts with 5 methods: registerRoutes, listen, normalizeRequest, writeResponse, getNativeServer
  - Platform adapters (@typokit/platform-node, platform-bun, platform-deno) are separate from server adapters — orthogonal concerns that combine freely
  - Native adapter uses compiled radix tree directly for O(k) route lookup; framework adapters translate the route table to framework-native registrations
  - Fastify and Express work on Bun via compat mode but don't use Bun-native APIs; Hono and Native work natively on all 3 runtimes
  - Framework-native middleware (CORS, compression) runs BEFORE TypoKit normalization; typed middleware runs AFTER
  - 351 pages total after build (same count — replaced a stub page)
---

## 2026-03-02T18:12 - US-013
- Wrote comprehensive Core Concept page for Database Adapters at packages/docs/src/content/docs/core-concepts/database-adapters.mdx
- Covers: philosophy (generate types, not queries — not an ORM), DatabaseAdapter interface (generate, diff, generateRepositories), comparison table of all 4 adapters (Drizzle, Kysely, Prisma, Raw SQL), full generated output examples for each, type metadata flow from JSDoc to DB schema, migration philosophy (drafts never auto-applied, destructive flagging), ownership boundary table, choosing an adapter guide
- Uses Starlight components: Aside (tip, note, caution)
- Cross-links kept minimal (no broken links to unwritten pages)
- Files changed: packages/docs/src/content/docs/core-concepts/database-adapters.mdx
- **Learnings for future iterations:**
  - DatabaseAdapter interface is in @typokit/core at packages/core/src/adapters/database.ts with generate(), diff(), and optional generateRepositories()
  - All 4 DB adapters support both PostgreSQL and SQLite dialects via constructor option
  - Drizzle generates per-table .ts files; Kysely generates single database.ts; Prisma generates schema.prisma; Raw SQL generates per-table .sql + types.ts
  - MigrationDraft includes destructive boolean flag — CI blocks on destructive migrations until reviewed
  - JSDoc tags @id, @generated, @unique, @maxLength, @default, @format, @table drive all database schema generation
  - Naming: Type→Table uses pluralize+snake_case, Property→Column uses snake_case
  - 351 pages total after build (same count — replaced a stub page)
---

## 2026-03-02T18:17 - US-014
- Wrote comprehensive Core Concept page for Plugins at packages/docs/src/content/docs/core-concepts/plugins.mdx
- Covers: TypoKitPlugin interface with all hooks, BuildPipeline with 6 async series hooks (beforeTransform→afterTypeParse→afterValidators→afterRouteTable→emit→done), runtime lifecycle hooks (onStart→onReady→onError→onStop→onSchemaChange), plugin registration in createApp(), why no onRequest/onResponse hooks (use middleware instead), official plugins (@typokit/plugin-debug with 8 debug endpoints, @typokit/plugin-ws with build-time codegen + runtime connection management), full custom plugin example with services/CLI commands/inspect endpoints, design guidelines
- Uses Starlight components: Aside (tip, note, caution)
- Cross-links to middleware-and-context, custom-plugin-development guide
- Files changed: packages/docs/src/content/docs/core-concepts/plugins.mdx
- **Learnings for future iterations:**
  - TypoKitPlugin interface is in packages/core/src/plugin.ts; BuildPipeline in packages/core/src/hooks.ts; createApp in packages/core/src/app.ts
  - Build pipeline uses AsyncSeriesHook (tapable pattern) with 6 phases — plugins tap via pipeline.hooks.{phase}.tap(name, fn)
  - Plugins register services on app.services during onStart (DI pattern); built-in services prefixed with underscore (_debug, _ws)
  - Plugin-debug exposes 8 endpoints on a separate sidecar port (default 9229) with dev/production security modes
  - Plugin-ws hooks into both build-time (afterTypeParse + emit for codegen) and runtime (onStart/onReady/onStop for connection management)
  - No onRequest/onResponse hooks by design — plugins use defineMiddleware() during onStart for per-request logic
  - 351 pages total after build (same count — replaced a stub page)
---

## 2026-03-02T18:27 - US-016
- Wrote comprehensive Core Concept page for Observability and Debugging at packages/docs/src/content/docs/core-concepts/observability.mdx
- Covers: structured logging via ctx.log with automatic traceId/route/phase enrichment, Logger API and log entry structure, log configuration with field redaction, log sinks (Stdout/OTel/DebugSidecar), OpenTelemetry integration (@typokit/otel) with auto-instrumented spans and span exporters, log-to-span bridging with OTel severity mapping, request lifecycle trace format (full JSON example), debug sidecar (@typokit/plugin-debug) with 12 endpoints and dev vs production security modes, typokit inspect CLI commands with --json flag, structured error context for AI agents with extended failure details, observability signal flow diagram
- Uses Starlight components: Aside (tip, note, caution)
- Cross-links to error-handling, middleware-and-context, plugins, building-first-api
- Files changed: packages/docs/src/content/docs/core-concepts/observability.mdx
- **Learnings for future iterations:**
  - StructuredLogger in @typokit/otel emits to 3 sinks simultaneously: Stdout, OtelLogSink, and DebugSidecar
  - OTel log severity mapping: trace=1, debug=5, info=9, warn=13, error=17, fatal=21
  - Debug sidecar has 12 endpoints (not 8 as mentioned in US-014 progress) -- includes /_debug/server, /_debug/build-pipeline, /_debug/spans, /_debug/logs
  - typokit inspect commands mirror debug sidecar endpoints but work from CLI without a running sidecar
  - Extended AI error context includes failures[] array with path, expected, received, suggestion fields
  - 351 pages total after build (same count -- replaced a stub page)
---

## 2026-03-02T18:33 - US-017
- Wrote comprehensive step-by-step tutorial at packages/docs/src/content/docs/guides/building-first-api.mdx
- Tutorial builds a Notes API (not todos) with 5 CRUD endpoints: POST/GET/GET/:id/PUT/:id/DELETE/:id
- Covers 10 steps: scaffold project, define entity type with JSDoc, define route contracts, implement handlers, add auth middleware with context narrowing, register routes in app.ts, generate database schema with Drizzle, start dev server and test with curl, run auto-generated contract tests, write integration tests
- Each step includes the file path, full code listing, and expected output
- Uses native server + Drizzle adapter (most common path per AC)
- Includes curl commands for every endpoint plus validation/auth error testing
- Uses Starlight components: Aside (tip, note, caution), Steps, Tabs/TabItem
- Cross-links to middleware-and-context, error-handling, server-adapters, observability, quickstart
- Files changed: packages/docs/src/content/docs/guides/building-first-api.mdx
- **Learnings for future iterations:**
  - Guide pages should use Steps component for numbered tutorial flow — gives clear visual progression
  - The existing stub files at guides/ are already configured in the sidebar at astro.config.mjs; no sidebar changes needed
  - AC said `first-api.mdx` but sidebar already references `building-first-api` slug — use the existing file
  - Tutorial content should go deeper than quickstart but reference it as prerequisite
  - 351 pages total after build (same count — replaced a stub page)
---

## 2026-03-02T18:39 - US-018
- Wrote comprehensive guide for building custom server adapters at packages/docs/src/content/docs/guides/custom-server-adapter.mdx
- Covers: full ServerAdapter interface with method-by-method explanation, key types (TypoKitRequest, TypoKitResponse, CompiledRoute, ServerHandle), 6-step walkthrough building a "Micro" adapter (package setup, collectRoutes tree walker, normalizeRequest, writeResponse, registerRoutes with middleware chain, wiring into app), compiled route table radix tree structure explanation, testing guidance with 4 test examples, common pitfalls section
- Uses Starlight components: Aside (tip, note, caution), Steps
- Cross-links to server-adapters concept page, middleware-and-context, building-first-api
- Files changed: packages/docs/src/content/docs/guides/custom-server-adapter.mdx
- **Learnings for future iterations:**
  - collectRoutes() is NOT an exported utility -- each adapter implements its own version internally; document this clearly
  - CompiledRouteTable is just an alias for CompiledRoute (the root node); the tree uses children/paramChild/wildcardChild
  - Route lookup priority is: static children -> parameterized child -> wildcard child
  - params field in TypoKitRequest starts empty in normalizeRequest(); URL params are injected by the framework router
  - ServerHandle.close() must be async -- important for graceful shutdown with request draining
  - 351 pages total after build (same count -- replaced a stub page)
---

## 2026-03-02T18:45 - US-019
- Wrote comprehensive custom plugin development guide at packages/docs/src/content/docs/guides/custom-plugin.mdx
- Builds a request-timing plugin (plugin-timing) step by step across 11 steps: project setup, plugin skeleton, build-time hooks (afterRouteTable), runtime services (app.services DI), onReady warmup, onError tracking, graceful shutdown, CLI commands (timing:report), introspection endpoints (/_inspect/timing), framework-native middleware via getNativeServer(), plugin registration in createApp()
- Includes build pipeline hook reference table, complete plugin source code, and design guidelines section
- Uses Starlight components: Aside (tip, note, caution), Steps
- Cross-links to plugins concept page, middleware-and-context, building-first-api
- Files changed: packages/docs/src/content/docs/guides/custom-plugin.mdx
- **Learnings for future iterations:**
  - AC says custom-plugin.mdx but sidebar slug is guides/custom-plugin — the stub file already existed at the correct path
  - Plugin state should live in closure scope (factory function), not on AppInstance — this is a key design pattern
  - Official plugins prefix services with underscore (_debug, _ws); custom plugins should use unprefixed names
  - defineMiddleware() priority: lower numbers run first; use negative priority for instrumentation middleware
  - getNativeServer() is available via app instance for framework-native middleware registration
  - 351 pages total after build (same count — replaced a stub page)
---

## 2026-03-02T18:55 - US-020
- Wrote comprehensive migration guide at packages/docs/src/content/docs/guides/migration.mdx
- Covers incremental adoption path for both Express and Fastify projects
- Express: install @typokit/server-express, pass existing app via expressServer({ app }), add TypoKit routes alongside legacy ones
- Fastify: install @typokit/server-fastify, use getNativeServer() to register existing plugins and legacy routes
- Explains middleware coexistence: framework-native middleware runs first (CORS, helmet, compression), TypoKit typed middleware runs after normalization for TypoKit routes only
- Includes request processing order diagram, migration checklist, FAQ section
- What changes vs stays the same comparison table
- Uses Starlight components: Aside (tip, note, caution), Steps, Tabs/TabItem
- Cross-links to server-adapters, middleware-and-context, error-handling, building-first-api
- Files changed: packages/docs/src/content/docs/guides/migration.mdx
- **Learnings for future iterations:**
  - Express adapter accepts existing app instance via expressServer({ app: myExistingApp }) — key migration pattern
  - Fastify adapter uses getNativeServer() for accessing native Fastify instance for plugins/legacy routes
  - Framework-native middleware runs BEFORE TypoKit normalization; TypoKit middleware runs AFTER — only for TypoKit routes
  - Legacy routes and TypoKit routes coexist because they're registered on the same framework instance
  - 351 pages total after build (same count — replaced a stub page)
---